# 배포 기록 2026-08-21 — 웹 전용 (집계표 혜택 문구)

## 범위

**웹만.** exe 빌드·R2 업로드·버전 올리기는 하지 않았다.
`66b7439..bdf39ba` 5커밋에 `js/` `server/` `electron/` 변경이 0건이라
설치파일을 새로 만들 이유가 없었다.

## 배포 사유

`bdf39ba` 가 `web/purchase.html` 을 고쳤으나 Pages 배포가 누락돼
저장소(26,224 B)와 배포본(26,240 B)이 어긋나 있었다. 주간 점검 2차에서 발견했다.

계획서 S0-2b 는 "완결"로 적혀 있었지만 그 완료 판정이
"`purchase.html` 의 (준비 중) 해제"라서, 배포 전까지는 공개 페이지 기준 미충족이었다.

## 실제 변경 — 3줄

| 위치 | 이전 | 이후 |
|---|---|---|
| 328행 | `모든 유료 플랜 공통 혜택 — 준비 중` | `모든 유료 플랜 공통 혜택` |
| 329행 | `외관조사 집계표 엑셀 양식 (준비 중)` | `외관조사 집계표 엑셀 양식` |
| 333행 | `위 두 가지는 아직 제공 준비 중이라 현재 구매 혜택에 포함되지 않습니다` | `집계표 엑셀 양식은 구매 확인 후 인증키 안내 메일에 함께 보내드립니다. 안드로이드 앱은 아직 준비 중…` |

## 실행

```
cd server && npx wrangler pages deploy ../web --project-name numdraw --branch main --commit-dirty=true
→ Uploaded 1 files (4 already uploaded)
→ https://c13c2366.numdraw.pages.dev
```

## 검증 결과

| 항목 | 결과 |
|---|---|
| 5페이지 HTTP | ✅ 전부 200 |
| 로컬 ↔ 배포본 바이트 | ✅ 5개 전부 일치 (purchase 26,224 = 26,224) |
| 배포본 ↔ 저장소 diff | ✅ 차이 없음 |
| 새 문구 반영 | ✅ 1건 / 옛 문구 0건 |
| 세 값 (앱·R2·홈페이지) | ✅ 1.2.4 / 1.2.4 / 1.2.4 (R2 미변경, 81,241,627 B 그대로) |

## ⚠️ 확인하지 못한 것 — 발급 전 반드시 해결

**이 PC 에 `외관집계표_양식.xlsx` 이 없다.**

이번 배포로 홈페이지가 "집계표 양식을 인증키 안내 메일에 함께 보내드립니다"를
**공개 약속**으로 바꿨고, `/issue-key` 절차도 첨부를 필수로 박아 두었다
(`.claude/skills/issue-key/SKILL.md:35`). 그런데 이 PC 에서 찾은 xlsx 는
`pic app/외관집계표_원본백업.xlsx` (원본 백업) 하나뿐이다.

원인은 정책상 의도된 것이다 — `.gitignore:51` 이 `외관집계표*.xlsx` 를 막는다
("유료 혜택 배포물, 공개 저장소에 올리지 않는다"). 양식은 다른 PC 에서 제작됐고
git 으로 따라오지 않는다.

- 물건 없는 공약은 아니다. 계획서 §2-2-c 가 제작·검증 완료를 근거와 함께 적었다.
- 다만 **이 PC 에서는 첨부할 파일이 없어 발급을 완주할 수 없다.**
- 현재 발급 대기 0건이라 즉시 사고는 아니지만, 첫 발급 전에 파일을 옮겨 두어야 한다.
- 저장소를 통한 이동은 금지(공개 저장소). USB·R2 비공개 경로·개인 클라우드로 옮긴다.
