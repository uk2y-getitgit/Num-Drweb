# 배포 기록 — v1.2.4 (2026-08-19)

계획서 **S0-1** 실행. 홈페이지(v1.2.4 표기)와 R2 설치파일(v1.2.3) 불일치 해소가 목적.

## 결과

| 항목 | 값 |
|---|---|
| 버전 | 1.2.4 |
| 빌드 | `dist/NumDraw Setup 1.2.4.exe` · 81,241,627 bytes (2026-08-18 빌드본 재사용) |
| 배포설정 가드 | `npm run checkrelease` 통과 (공개키 43자·활성화 주소·문의처 주입 확인) |
| R2 | `https://pub-a66add04be154b7e97486436051b6b2e.r2.dev/NumDraw-Setup-1.2.4.exe` → **200**, Content-Length 81,241,627 (로컬 파일과 일치) |
| 홈페이지 | `web/purchase.html` — `DOWNLOAD_URL`, `#dlFile` 표기(파일명·용량 81MB) 갱신 |
| Pages | 배포 완료 · `https://4df7f653.numdraw.pages.dev` → 프로덕션 `numdraw.pages.dev` 반영 확인 |
| 서버 테스트 | `cd server && npm test` — **71건 전체 통과** |

**세 값 일치**: `package.json` 1.2.4 = R2 파일명 1.2.4 = 홈페이지 표기 1.2.4 ✅

## 재빌드하지 않은 근거

8월 18일 빌드본이 현재 `main` 과 같은 코드인지 파일 단위로 대조했다.
`index.html` · `js/app.js` · `js/photoBook.js` · `css/ui-skin.css` · `css/ui-tokens.css` ·
`electron/license/pubkey.js` 6개가 저장소와 패키징본(`dist/win-unpacked/resources/app/`)에서
md5 동일, 패키징본 `package.json` 버전도 1.2.4. UI 개편 브랜치를 main 에 병합할 때
충돌은 `CLAUDE.md` · `package.json` 문서·메타뿐이었으므로 앱 코드는 그대로다.

## 확인 못 함

- 앱 수동 체크리스트 10항목 (넘버링·사진매칭·사진첩 PDF·체험판 저장 게이트 등) — 이번 배포는
  코드 변경이 없어 게이트로 걸지 않았다. 다음 코드 변경 배포 전에는 `/qa` 로 확인할 것.
- 라이선스 5종(2대 제한·좌석 반납·정지·오프라인 재실행·체험판 게이트) — 두 번째 PC 필요, S1 게이트.

## 남은 것

- 구버전 파일(`NumDraw-Setup-1.2.3.exe`, `1.2.0`)은 버킷에 그대로 둔다 — 이미 링크를 받은 사람이 있을 수 있다.
- 다운로드 시 브라우저 평판 경고는 새 파일이라 다시 뜬다. 근본 해결은 코드서명(트리거: 누적 30건).
