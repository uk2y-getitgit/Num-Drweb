# NumDraw 유료화 — 배포 따라하기 (비개발자용)

작성: 2026-08-10

이 문서는 **코드를 몰라도 그대로 따라 할 수 있게** 만든 것이다.
각 단계마다 **"이 화면이 나오면 성공"** 과 **"이 화면이 나오면 실패, 이렇게 하세요"** 를 적어 두었다.

> **중요:** 단계를 건너뛰지 마세요. 앞 단계가 성공해야 뒤 단계가 의미가 있습니다.
> 실패하면 그 자리에서 멈추고, 화면에 나온 글자를 그대로 복사해서 물어보면 됩니다.

---

## 0. 먼저 알아둘 것

### 지금까지 만들어진 것

| 무엇 | 상태 |
|---|---|
| NumDraw 앱 (넘버링 기능) | ✅ 완성 |
| 라이선스 인증 기능 (앱 안쪽) | ✅ 완성, 보안 검수 2회 통과 |
| 활성화 서버 (키 확인·좌석 관리) | ✅ 완성, 검증 71건 통과 |
| 키 발급 관리자 화면 | ✅ 완성 |
| **인터넷에 서버 올리기** | ❌ **아직 안 함 — 이 문서의 2단계** |
| **설치파일(.exe) 만들기** | ❌ **한 번도 안 해봄 — 이 문서의 1·4단계** |
| 판매 홈페이지 | ❌ 아직 (다음 Phase) |
| 가격 | ❌ **미정 — 홈페이지 만들기 전에 정해야 함** |

### 준비물

| 항목 | 설명 | 비용 |
|---|---|---|
| Cloudflare 계정 | 서버를 올릴 곳. 이메일만 있으면 가입됨 | **무료** |
| 이메일 주소 (고객 문의용) | 앱에서 "문의: ..." 로 표시됨 | — |
| 비밀번호 저장할 곳 | 잃어버리면 키 발급을 못 함 | — |
| 시간 | 처음부터 끝까지 약 2시간 | — |

무통장입금 수동 처리 방식이라 **결제대행사(PG) 가입이 필요 없습니다.**

### 명령어 입력하는 법

1. 작업 폴더에서 마우스 **우클릭 → "터미널에서 열기"**
   (안 보이면 `Shift` + 우클릭)
2. 검은 창(PowerShell)이 뜨면 아래 명령을 **복사해서 붙여넣고 Enter**

명령이 여러 줄이면 **한 줄씩** 실행하세요.

---

## 1단계. 설치파일이 만들어지는지 먼저 확인 (약 20분)

**왜 먼저 하나:** 설치파일(.exe) 만들기는 이 프로젝트에서 **한 번도 해본 적이 없습니다.**
서버까지 다 올려놓고 나서 여기서 막히면 헛수고가 되니, 돈도 계정도 필요 없는 이 단계부터 확인합니다.

이 단계에서 만드는 건 **체험판 전용 시험 빌드**입니다. 판매용이 아닙니다.

### 1-1. 지금 코드가 정상인지 확인

```
cd server
npm test
cd ..
```

**✅ 성공 화면**
```
==============================================================
  전체 통과 — 71건
==============================================================
```

**❌ 실패하면** — 화면에 `✕` 로 시작하는 줄이 나옵니다. 그 줄들을 복사해서 물어보세요.
여기서 실패하면 **다음 단계로 가지 마세요.**

---

### 1-2. 시험용 설치파일 만들기

지금은 서버 주소와 인증 열쇠가 비어 있어서, 그냥 만들면 **일부러 중단**됩니다.
(그래야 나중에 실수로 "아무도 정품 인증이 안 되는 설치파일"을 파는 사고를 막습니다.)

시험용이라고 알려주고 만듭니다. **아래 3줄을 순서대로** 실행하세요.

```
$env:NUMDRAW_ALLOW_UNCONFIGURED_BUILD=1
npm run build
$env:NUMDRAW_ALLOW_UNCONFIGURED_BUILD=$null
```

> 마지막 줄은 **반드시** 실행하세요. 안 하면 나중에 진짜 빌드할 때도 검사가 꺼진 채로 나갑니다.

**✅ 성공 화면** — 중간에 이런 경고가 나오는 게 정상입니다:
```
  라이선스 설정 미완료 (강제 진행 중)
  ⚠ 이 설치파일로는 아무도 정품 인증을 할 수 없습니다 (전원 체험판).
```
그리고 마지막에 `dist` 폴더가 생기고 그 안에 **`NumDraw Setup 1.2.0.exe`** 같은 파일이 있으면 성공입니다.

**❌ 자주 나는 실패**

| 화면에 나온 말 | 뜻 | 해결 |
|---|---|---|
| `electron-builder` 를 찾을 수 없음 | 부품이 안 깔림 | `npm install` 먼저 실행 |
| `EPERM` / `EBUSY` / 권한 오류 | 백신이나 열려 있는 앱이 파일을 잡고 있음 | NumDraw 앱을 모두 닫고 다시 시도. 그래도 안 되면 백신 실시간 검사 잠시 끄기 |
| `dist` 폴더 삭제 실패 | 이전 결과물이 열려 있음 | 탐색기에서 `dist` 폴더 창을 닫고 다시 시도 |
| 다운로드가 멈춤 | 처음 빌드 때 전자 부품을 인터넷에서 받음 | 인터넷 연결 확인 후 재시도 (한 번 받으면 다음부터 빠름) |

---

### 1-3. 만든 설치파일을 실제로 설치해서 확인

`dist` 폴더의 `.exe` 를 더블클릭해 설치하고 실행합니다.

**확인할 것 — 지금은 "체험판"이 맞습니다**

| 확인 항목 | 기대 결과 |
|---|---|
| 앱이 켜지는가 | ✅ 켜져야 함 |
| 도면 불러오기·넘버링이 되는가 | ✅ 평소처럼 되어야 함 |
| PDF 저장 시 워터마크 | ✅ **찍혀야 정상** (체험판이므로) |
| 페이지 여러 장 PDF 저장 | ✅ **1장만 나와야 정상** (체험판이므로) |

> 워터마크가 **안** 찍히거나 여러 장이 저장되면 그게 오히려 문제입니다.
> 체험판 제한이 안 걸린 것이므로 알려주세요.

**여기까지 성공했다면 1단계 완료입니다.** 설치파일을 만들 수 있다는 게 확인됐습니다.
설치한 시험판은 지워도 되고 놔둬도 됩니다.

---

## 2단계. 서버를 인터넷에 올리기 (약 30분)

**무엇을 하나:** 고객이 라이선스 키를 입력하면 "이 키가 진짜인지, 몇 대까지 썼는지" 확인해 줄
서버를 Cloudflare 라는 회사의 무료 서비스에 올립니다.

### 2-1. Cloudflare 가입

1. https://dash.cloudflare.com/sign-up 접속
2. 이메일 + 비밀번호로 가입 (카드 등록 필요 없음)
3. 메일함에서 인증 메일 확인

### 2-2. 로그인 연결

```
cd server
npx wrangler login
```

브라우저 창이 자동으로 뜹니다. Cloudflare 로그인 후 **"Allow"** 를 누르세요.

**✅ 성공 화면**
```
Successfully logged in.
```

확인:
```
npx wrangler whoami
```
본인 이메일이 보이면 성공입니다.

---

### 2-3. 데이터베이스 만들기

키 발급 내역과 활성화 기록을 저장할 곳입니다.

```
npx wrangler d1 create numdraw-license
```

**✅ 성공 화면** — 이런 내용이 나옵니다:
```
[[d1_databases]]
binding = "DB"
database_name = "numdraw-license"
database_id = "1a2b3c4d-5e6f-...."   ← 이 값을 복사!
```

**할 일:** `server` 폴더의 **`wrangler.toml`** 파일을 메모장으로 열어서,
`database_id = "00000000-0000-..."` 부분을 방금 복사한 값으로 바꾸고 저장합니다.

> 이 값은 비밀번호가 아닙니다. 노출돼도 안전합니다.

---

### 2-4. 데이터베이스에 표 만들기

```
npx wrangler d1 execute numdraw-license --remote --file=schema.sql
```

> `--remote` 를 **꼭** 붙이세요. `--local` 을 쓰면 이 PC에서 오류가 납니다 (알려진 문제).

**✅ 성공 화면**: `Executed 3 commands` 같은 메시지

---

### 2-5. 인증 열쇠 만들기 ⚠ 가장 중요한 단계

```
npm run genkeys
```

화면에 **공개키**와 **개인키** 두 개가 나옵니다.

| 열쇠 | 성격 | 어디에 넣나 |
|---|---|---|
| **공개키** | 노출돼도 안전 | 앱 안에 넣음 (3단계에서) |
| **개인키** | **절대 비밀** | Cloudflare 서버에만 넣음 (지금) |

**지금 바로 할 일 — 창을 닫지 마세요:**

1. **공개키**를 복사해서 메모장에 임시로 붙여넣어 둡니다 (3단계에서 씀)
2. 개인키를 서버에 등록합니다:
   ```
   npx wrangler secret put CERT_PRIVATE_KEY
   ```
   `Enter a secret value:` 가 뜨면 **개인키**를 붙여넣고 Enter

3. 관리자 화면 비밀번호를 만듭니다. **직접 짓지 말고** 아래 명령으로 만드세요:
   ```
   [Convert]::ToBase64String((1..32|%{Get-Random -Max 256}))
   ```
   나온 값을 복사해서 **비밀번호 관리 프로그램이나 안전한 곳에 저장**한 뒤:
   ```
   npx wrangler secret put ADMIN_TOKEN
   ```
   붙여넣고 Enter

   > ⚠ **24자 미만이면 관리자 화면이 아예 안 열립니다.** 위 명령이 만든 값을 그대로 쓰세요.
   > 이 비밀번호를 잃어버리면 키 발급을 못 합니다. 꼭 저장하세요.

4. **개인키가 화면에 남아 있습니다.** 이 검은 창을 닫으세요.

---

### 2-6. 서버 올리기

```
npm run deploy
```

**✅ 성공 화면**
```
Published numdraw-activation
  https://numdraw-activation.○○○○.workers.dev
```

**이 주소를 메모장에 적어 두세요.** 앞으로 계속 씁니다.

**❌ 실패하면**

| 화면에 나온 말 | 해결 |
|---|---|
| `database_id` 관련 오류 | 2-3에서 `wrangler.toml` 수정을 안 했거나 잘못 붙여넣음 |
| `Authentication error` | 2-2 로그인 다시 |

---

### 2-7. 서버가 살아 있는지 확인

브라우저 주소창에 위에서 받은 주소를 그대로 입력합니다.

**✅ 성공**: `NumDraw activation server OK` 라는 글자가 보임

이어서 주소 뒤에 **`/admin/issue`** 를 붙여서 들어갑니다.
예: `https://numdraw-activation.○○○○.workers.dev/admin/issue`

**✅ 성공**: 로그인 창이 뜸 → 아이디는 아무거나(예: `admin`), 비밀번호는 2-5의 `ADMIN_TOKEN`
→ **"새 라이선스 키 발급"** 화면이 보이면 성공입니다.

**❌ 실패**

| 화면 | 뜻 | 해결 |
|---|---|---|
| `관리자 화면이 비활성화되어 있습니다` | ADMIN_TOKEN이 24자 미만 | 2-5의 3번을 다시, 더 긴 값으로 |
| 로그인 창이 계속 다시 뜸 | 비밀번호가 틀림 | 저장해 둔 값 확인. **8번 틀리면 15분 잠깁니다** — 15분 기다리거나 `npm run deploy` 재실행 |
| `로그인 시도가 너무 많습니다` | 위 잠금에 걸림 | 15분 대기 또는 `npm run deploy` |

---

## 3단계. 앱에 서버 정보 넣기 (약 10분)

메모장으로 파일 2개를 고칩니다.

### 3-1. 공개키 넣기

**파일:** `electron\license\pubkey.js`

24번째 줄쯤에 이런 줄이 있습니다:
```js
const PUBLIC_KEY = 'REPLACE_WITH_ED25519_PUBLIC_KEY';
```

`REPLACE_WITH_ED25519_PUBLIC_KEY` 부분만 2-5에서 복사해 둔 **공개키**로 바꿉니다.
**작은따옴표 `'` 와 세미콜론 `;` 은 지우지 마세요.**

바꾼 뒤 예시:
```js
const PUBLIC_KEY = 'DXV3YuC4UkvaWYRBL00cLy9nb7VNTKcwRyITxCUe3oU';
```

### 3-2. 서버 주소·문의처 넣기

**파일:** `electron\license\config.json`

```json
{
  "activationUrl": "",
  "supportContact": "",
  "enforceKeyChecksum": true
}
```

이렇게 채웁니다 (주소 뒤에 **`/api/activate`** 를 꼭 붙이세요):
```json
{
  "activationUrl": "https://numdraw-activation.○○○○.workers.dev/api/activate",
  "supportContact": "본인이메일@example.com",
  "enforceKeyChecksum": true
}
```

> 큰따옴표 `"` 와 쉼표 `,` 를 지우면 앱이 못 읽습니다. 값만 바꾸세요.

### 3-3. 제대로 넣었는지 확인

```
node scripts/check-release-config.mjs
```

**✅ 성공 화면**
```
✓ 라이선스 배포 설정 확인 완료 — 빌드를 진행합니다.
```

**❌ 실패 화면** — 무엇이 잘못됐는지 한글로 알려줍니다:
```
  ✕ 공개키가 아직 자리표시자입니다 ...
  ✕ activationUrl 이 비어 있습니다 ...
```
나온 항목만 고치고 다시 실행하세요.

---

## 4단계. 판매용 설치파일 만들기 (약 10분)

이제 **1단계와 달리** 아무 옵션 없이 그냥 만듭니다.

```
npm run build
```

**✅ 성공 화면**: 맨 처음에 `✓ 라이선스 배포 설정 확인 완료` 가 나오고,
`dist` 폴더에 `NumDraw Setup 1.2.0.exe` 가 생깁니다.

**❌ 중단되면**: 3단계를 다시 확인하세요. 중단됐다는 건 **설정이 덜 됐다는 뜻이고,
그대로 팔았으면 구매자 전원이 인증 실패했을 상황을 막은 것입니다.**

---

## 5단계. 판매 시뮬레이션 — 실전 테스트 (약 30분) ⚠ 생략 금지

**여기가 가장 중요합니다.** 지금까지의 검증은 전부 "흉내"였고,
**진짜 서버에서 진짜 앱으로 돌려본 적은 한 번도 없습니다.**
첫 고객에게 팔기 전에 반드시 본인이 먼저 고객 역할을 해보세요.

### 5-1. 키 발급 (판매자 역할)

1. `https://<서버주소>/admin/issue` 접속 → 로그인
2. 구매자 이름에 `테스트`, 메모에 `내부 테스트` 입력
3. **최대 활성화 대수는 2** 그대로 두고 "키 발급" 클릭
4. 화면에 나온 키(`ND2-XXXXX-XXXXX-XXXXX-XXXXX`)를 **메모장에 복사**

> ⚠ 이 화면을 벗어나면 키를 **다시 볼 수 없습니다.** 반드시 지금 복사하세요.

### 5-2. 활성화 (구매자 역할)

1. 4단계에서 만든 설치파일을 설치하고 앱 실행
2. 라이선스 입력 화면에서 복사한 키 붙여넣기
3. 인증 클릭

| 확인 항목 | 기대 결과 |
|---|---|
| 인증 성공 메시지 | ✅ 정품으로 전환 |
| PDF 저장 시 워터마크 | ✅ **없어야 함** |
| 여러 장 PDF 저장 | ✅ **전부 저장돼야 함** |

**❌ 여기서 실패하면 — 원인별 대처**

| 화면에 나온 말 | 뜻 | 해결 |
|---|---|---|
| `서버가 보낸 증서를 확인할 수 없습니다` | **공개키와 개인키가 짝이 안 맞음** | `npm run genkeys` 를 두 번 돌렸을 가능성. 2-5부터 다시 (한 번에 나온 공개키·개인키 쌍을 써야 함) |
| `활성화 서버 주소가 설정되지 않았습니다` | config.json 이 비었거나 4단계 전 빌드 | 3-2 확인 후 다시 빌드 |
| `인터넷에 연결할 수 없습니다` | 주소 오타 | config.json 의 주소 끝에 `/api/activate` 있는지 확인 |
| `키를 다시 확인해 주세요` | 키 오타 | 복사한 키 다시 붙여넣기 |
| 서버 오류(500) | **서버가 Ed25519 서명을 못 함** | 이건 확인 못 한 항목입니다. 이 메시지가 나오면 바로 알려주세요 |

> 마지막 항목은 미확인 위험입니다 — Cloudflare 서버가 이 서명 방식을 지원하는지
> 이 PC에서는 확인할 방법이 없었습니다. **5-2에서 바로 드러납니다.**

### 5-3. 재설치해도 좌석이 안 늘어나는지

1. 앱을 **삭제(제거)** 하고 다시 설치
2. 같은 키로 다시 인증

**✅ 기대:** 성공하고, 관리자 화면 `/admin/list` 에서 좌석이 **여전히 1/2**

### 5-4. 2대 제한이 실제로 걸리는지

가능하면 **다른 PC**(노트북 등)에서:

1. 같은 설치파일로 설치 → 같은 키로 인증 → **성공, 좌석 2/2**
2. 또 다른 PC에서 인증 → **`이 라이선스는 이미 2대에서 사용 중입니다` 가 떠야 정상**

> 다른 PC가 없으면 이 단계는 건너뛰되, **첫 판매 전에는 꼭 한 번 해보세요.**
> 여기가 수익과 직결됩니다.

### 5-5. PC 교체 대응 (좌석 반납)

1. `/admin/list` 에서 해당 키의 기기 옆 **"반납"** 클릭
2. 그 PC에서 다시 인증 → **성공해야 정상**

> 예전에 여기서 오류(500)가 나는 버그가 있었고 고쳤습니다. 실제로 확인하는 단계입니다.

### 5-6. 키 정지

1. `/admin/list` 에서 **"정지"** 클릭
2. **새로운** PC에서 그 키로 인증 → **거부돼야 정상**

> ⚠ **이미 인증된 PC는 계속 정품으로 동작합니다. 이건 버그가 아니라 설계입니다.**
> 인터넷 없이 쓸 수 있게 만든 대가입니다. 고객에게 "정지했으니 이제 못 쓴다"고
> 안내하면 사실과 다릅니다. 정지는 **추가 확산 차단**용입니다.

---

## 6. 5단계까지 끝나면 — 판매 개시 전 남은 일

| 할 일 | 누가 | 비고 |
|---|---|---|
| **가격 결정** | 사장님 | 홈페이지 만들기 전 필수 |
| 사업자등록 · 통신판매업 신고 | 사장님 | 계속 판매하려면 필요 |
| 입금받을 계좌 정리 | 사장님 | 무통장입금 수동 처리 |
| 판매 홈페이지 | 개발 | 가격 정해진 뒤 착수 |
| 이용약관·개인정보처리방침·환불정책 | 개발 | 판매 개시 전 게시 필요 |
| 사용가이드 | 개발 | |

---

## 7. 평상시 운영 (판매 시작 후)

| 상황 | 할 일 |
|---|---|
| 입금 확인됨 | `/admin/issue` 에서 키 발급 → 메일로 전달 |
| 고객이 PC 바꿈 | `/admin/list` 에서 옛 기기 **반납** |
| 고객이 키 분실 | 평문 키는 저장 안 됨 → 옛 키 **정지** 후 **새 키 발급** |
| 부정 사용 의심 | **정지** (단, 이미 쓰던 PC는 계속 동작 — 5-6 참고) |

자세한 운영 절차는 `server\README.md` 에 있습니다.

---

## 8. 막혔을 때

1. 화면에 나온 글자를 **그대로 복사**
2. 몇 단계에서 막혔는지 함께 알려주기 (예: "2-6에서 이런 오류")

되돌리고 싶으면:
- 앱 설정을 원래대로: `git checkout -- electron/license/pubkey.js electron/license/config.json`
- 서버를 다시 올리기: `cd server` 후 `npm run deploy`

**절대 하지 말 것**
- `git reset --hard` — 지금까지 작업이 전부 사라집니다
- 개인키(`CERT_PRIVATE_KEY`)를 메모장·메일·저장소에 남기기
- `ADMIN_TOKEN` 을 짧고 외우기 쉬운 값으로 바꾸기
