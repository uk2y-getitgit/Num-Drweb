# 리브랜딩 실행계획 — NumDraw → Quickspect

> 작성 2026-08-21. 조사 근거는 본문 §2. 실행 전이며, 착수 승인 대기 상태다.

## 0. 왜 지금인가

세 조건이 동시에 열려 있고, 셋 다 곧 닫힌다.

| 조건 | 지금 | 닫히면 |
|---|---|---|
| 유료 고객 0명 | 라이선스·저장파일 호환을 신경 안 써도 된다 | 첫 판매 후에는 전부 마이그레이션 대상 |
| 도메인 미구매 | 새 이름으로 바로 산다 | `numdraw.co.kr` 을 사고 나면 매몰비용 |
| 안드로이드 새 앱 등록 예정 | 패키지명을 새로 정하면 **추가 비용 0** | 기존 패키지로 등록하면 되돌릴 수 없다 |

`appId` 는 특히 **지금이 마지막 기회**다. 바꾸는 순간 기존 설치본과 별개 앱이 되므로,
고객이 생긴 뒤에는 영원히 못 바꾼다.

---

## 1. 이름 체계

```
회사    골드랜드 (Goldland)          ← 사업자·DUNS·도메인 등록명의. 안 바뀐다
브랜드  Quickspect (퀵스펙트)        ← quick + inspect
약칭    퀵스
제품    ─ 퀵스도면  Quickspect Draw   PC. 도면 넘버링 + 사진첩   (구 NumDraw)
        ─ 퀵스샷    Quickspect Shot   안드로이드. 사진 번호 자동입력 (구 pic-numinc)
        ─ 퀵스프로  Quickspect Pro    유료판 표기
        ─ (빈자리)  Quickspect Report 확장 예정
```

명함·견적서·설치파일은 `Quickspect`, 현장 구전은 `퀵스`.

---

## 2. 조사 결과 (2026-08-21)

총 **338건** — `NumDraw` 186 / `numdraw` 135 / `NUMDRAW` 17.

| 영역 | 건수 | 처리 |
|---|---|---|
| 루트 문서·계획서류 | ~178 | 치환 |
| 하네스 `.claude/` | 54 | 치환 |
| 웹 5페이지 `web/` | 43 | 치환 |
| **과거 기록** `docs/archive/`·`운영기록/` | 41 | **손대지 않는다** |
| Electron `electron/`·`package.json` | 32 | 식별자 포함, 개별 판단 |
| 서버 `server/` | 31 | 인프라 이름, 대부분 유지 |
| 앱 코드 `js/`·`index.html` | 31 | 저장 키·확장자 포함 |

> **과거 기록을 바꾸지 않는 이유**: 배포기록·주간점검은 "그때 무엇을 했는가"의 사실 기록이다.
> 소급해서 이름을 갈면 기록이 거짓이 된다.
>
> **제외 대상 (확정)**: `docs/archive/` · `운영기록/` · `유료화_배포_따라하기.md` ·
> `유료화_작업재개_인수인계.md` · `프로그램_유료화_개발계획서.md` ·
> `사업화_실행계획.md:74`(R2 에 올라간 구 설치파일명).
> 뒤의 셋은 1.2.0~1.2.3 빌드 결과·날짜가 박힌 과거 작업 기록이라 `docs/archive/` 와 같은 성격이다.

---

## 3. 확정된 결정

| # | 항목 | 결정 | 이유 |
|---|---|---|---|
| D1 | 파일 확장자 | **`.qspec` + 하위호환** | 새 파일은 `.qspec`, 읽기는 둘 다 지원 |
| D2 | R2 버킷 | **`numdraw-download` 유지, 파일명만 교체** | 버킷명은 URL 중간 경로라 고객 눈에 안 보인다. 81MB 재업로드 회피 |
| D3 | Workers | **`numdraw-activation` 유지 + 커스텀 도메인** | 이름을 바꾸면 `activationUrl` 이 바뀌어 기존 활성화가 깨진다 |
| D4 | 라이선스 `product` 필드 | **`'numdraw'` 유지** | 사용자에게 안 보이는 내부 값. 바꾸면 발급된 증서가 전부 무효 |
| D5 | localStorage·IndexedDB 키 | **유지** | 바꾸면 자동저장·세션복원이 유실된다 |
| D6 | `appId` | **`com.goldland.quickspect` 로 변경** | 지금이 마지막 기회 |
| D7 | D1 데이터베이스 이름 | **유지** | 이름만 바꾸는 것은 무의미 |
| D8 | 기기 지문 `SALT` | **유지** | `fingerprint.js:17` 의 HMAC 키. 바꾸면 **활성화된 PC 가 전부 다른 기기로 인식**된다 |
| D9 | DOM id · dataTransfer 타입 · 환경변수명 | **유지** | `numdraw-file-input` · `numdraw/page-id` · `NUMDRAW_ALLOW_UNCONFIGURED_BUILD`. 전부 내부 식별자 |

---

## 4. 단계

### R0 — 상표 확인 (게이트 · 사람)

키프리스에서 `퀵스` · `퀵스펙트` · `Quickspect` 를 **9류(소프트웨어) · 42류(SW개발)** 로 검색한다.
퀵서비스 업체가 39류(운송)에 갖고 있어도 류가 다르면 공존 가능하다.

- **완료 판정**: 9류·42류에 동일·유사 상표 없음을 확인
- **막히면**: 이 계획 전체를 중단하고 이름을 다시 고른다. **R1 이후는 R0 통과 전에 착수하지 않는다.**

### R1 — 도메인 확보 (사람)

| 도메인 | 용도 | 상태 |
|---|---|---|
| `quickspect.com` | 본진 | 🟢 조회 시점 미등록 |
| `quickspect.kr` | 국내 | 🟢 |
| `quicks.co.kr` | 약칭 리다이렉트(선택) | 🟢 |

- **등록 명의는 반드시 골드랜드(313-14-63489)** — 구글 조직 계정 심사에서
  whois 등록자와 DUNS 조직명이 일치해야 한다
- `.kr` 계열은 Cloudflare Registrar 미취급 → 국내 등록기관에서 사고 네임서버만 Cloudflare 로
- **완료 판정**: whois 등록자에 골드랜드가 보인다

### R2 — 앱 코드 (제가 처리)

**a. 빌드 식별자** — `package.json`
- `name` `numdraw` → `quickspect`
- `appId` `com.numdraw.app` → `com.goldland.quickspect`
- `productName` · `nsis.shortcutName` `NumDraw` → `Quickspect`
- `build:admin` 스크립트의 `--config.appId` · `productName` · `shortcutName` 동반 수정

**b. 파일 연결 하위호환** — D1
- `package.json` `fileAssociations` 에 `qspec` 항목 추가, `numdraw` 항목은 남긴다 (둘 다 열림)
- `electron/main.js:16` · `:25` — `/\.numdraw$/i` → `/\.(qspec|numdraw)$/i`
- `index.html:70` — `accept=".numdraw"` → `accept=".qspec,.numdraw"`
- `js/storageManager.js:250` — `suggestedName` 을 `.qspec` 으로
- `js/storageManager.js:260` — `accept` 를 `['.qspec','.numdraw']` 로
- `js/app.js:1255` · `:1439` — PDF 기본 파일명 `'numdraw'` → `'quickspect'`

**c. 건드리지 않는 것** (D4·D5·D8·D9)
- `electron/license/activate.js:111` · `verify.js:5`·`:77` 의 `product:'numdraw'`
- `electron/license/fingerprint.js:17` 의 `SALT = 'NumDraw/device-fingerprint/v1'` ← **특히 위험**
- `js/storageManager.js:12`·`:15`·`:18`·`:36`·`:52` 의 `numdraw_tabId` · `numdraw_session_*` · `numdraw_db`
- `index.html:17`·`:763` 의 `numdraw-theme`
- `index.html:70` + `js/app.js:1173`·`:1176` 의 DOM id `numdraw-file-input`
- `js/app.js:1573`·`:1589` 의 `numdraw/page-id`
- `scripts/check-release-config.mjs` 의 `NUMDRAW_ALLOW_UNCONFIGURED_BUILD`

> 이 세 곳은 **의도적으로 남긴다.** 나중에 "치환이 빠졌다"고 오해하지 말 것.

- **완료 판정**: 앱 실행 → 창 제목이 Quickspect, `.qspec` 저장 성공, 기존 `.numdraw` 파일도 열림

### R3 — 문서·하네스 (제가 처리)

- 루트 `.md`, `.claude/`, `CLAUDE.md` 치환
- **`docs/archive/` · `운영기록/` 는 제외** (§2)
- `.claude/skills/release/SKILL.md` · `agents/release-pilot.md` 의 설치파일명 예시 갱신
- **완료 판정**: 과거 기록을 뺀 영역에 `numdraw` 잔여 0건 (내부 식별자 제외)

### R4 — 홈페이지 (제가 처리 + 승인)

- `web/` 5페이지 문구·로고·타이틀 치환
- `DOWNLOAD_URL` 을 새 설치파일명으로
- Pages 커스텀 도메인 연결 (`quickspect.com`)
- **완료 판정**: 새 도메인으로 5페이지 200, 저장소↔배포본 바이트 일치

### R5 — 빌드·배포 (`/release` · 승인 2회)

1. 버전을 **1.3.0** 으로 올린다 (리브랜딩은 마이너 이상)
2. `npm run checkrelease` → `npm run build`
3. **[승인]** R2 업로드 — 버킷은 그대로, 파일명만 `Quickspect-Setup-1.3.0.exe` (D2)
4. **[승인]** Pages 배포
5. 세 값 일치 확인
- **완료 판정**: `package.json` · R2 파일명 · 홈페이지 표기가 모두 1.3.0

### R6 — 안드로이드 (별도 저장소 · 조직 계정 등록 시)

`AI활용/pic-numinc` 에서 처리한다. 이 저장소가 아니다.
- 패키지명 `com.goldland.quickspect.shot` 으로 확정
- 앱 표시명 `퀵스샷`
- **어차피 새 앱을 등록하므로 추가 비용이 없다.** `안드로이드_출시_실행계획.md` §6-3 과 맞물린다

---

## 5. 순서 제약

```
R0 상표 ──┬─→ R1 도메인 ──→ R4 홈페이지(커스텀 도메인) ──┐
          │                                              ├─→ R5 빌드·배포
          └─→ R2 앱코드 ──→ R3 문서 ────────────────────┘
                                                          ↓
                                    R6 안드로이드 (조직 계정 등록 시)
```

- **R0 를 통과하기 전에는 아무것도 착수하지 않는다.** 상표가 막히면 전부 헛일이 된다
- R1 은 DUNS 임계경로와 겹친다 — 도메인은 조직 계정·통신판매업 신고의 공통 선행조건이므로
  `안드로이드_출시_실행계획.md` §2 와 같은 작업이다. **한 번 사면 양쪽이 같이 풀린다**
- R2·R3 은 R1 과 무관하게 병행 가능

## 6. 위험

| 위험 | 대응 |
|---|---|
| 상표 거절 | R0 게이트에서 걸러낸다 |
| 기존 `.numdraw` 파일이 안 열림 | D1 하위호환. R2 완료 판정에 "기존 파일 열림"을 포함했다 |
| 활성화가 깨짐 | D3·D4 로 `activationUrl` 과 `product` 를 그대로 둔다 |
| 자동저장 유실 | D5 로 저장 키를 그대로 둔다 |
| `appId` 변경으로 구버전이 남음 | 고객 0명이라 영향 없음. 본인 PC 의 구버전은 수동 제거 |
| 기기 지문이 바뀌어 활성화가 전부 깨짐 | D8 로 `SALT` 를 그대로 둔다. **치환 스크립트를 `electron/license/` 에 돌리지 말 것** |
| 과거 기록 오염 | R3 에서 `docs/archive/` · `운영기록/` 제외 |

## 6-1. 실행 중 발생한 사고 (2026-08-21)

R3 일괄 치환이 `electron/license/fingerprint.js` 의 `SALT` 까지 바꿨다.
`git grep -l NumDraw` 로 대상을 잡으면서 `electron/license/` 를 빼지 않은 것이 원인이다.
즉시 되돌리고 해당 줄에 경고 주석을 박았다.

**교훈**: 치환 대상을 "무엇을 포함할까"로 잡지 말고 **"무엇을 뺄까"를 먼저 확정**한 뒤 돌린다.
`electron/license/` 는 통째로 제외 대상이다.

## 7. 완료 판정 (전체)

- [ ] R0 키프리스 9류·42류 확인
- [ ] R1 도메인 3종, whois 등록자 = 골드랜드
- [x] **R2 코드 수정 완료 2026-08-21** — JS 문법검사 5파일 통과, `package.json` 유효,
      빌드가드 통과. **앱 실행·`.qspec` 저장·구 파일 열기는 사람이 확인해야 한다**
- [x] **R3 완료 2026-08-21** — 56+16건 치환. 서버 테스트 71건 전체 통과(회귀 없음).
      잔여 `NumDraw` 는 전부 제외 대상(web/ 은 R4, 과거기록, SALT)
- [ ] R4 새 도메인 5페이지 200
- [ ] R5 세 값 1.3.0 일치
- [ ] R6 패키지명 확정 (조직 계정 등록 시)
