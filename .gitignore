# Claude Code — 하네스(에이전트·스킬)는 버전관리한다. 기기별 로컬 설정만 제외
.claude/settings.local.json
.claude/*.local.*

# OS
.DS_Store
Thumbs.db
desktop.ini

# 빌드 결과물 (Electron)
dist/
dist-admin/
node_modules/
out/

# 환경 설정
*.local
.env

# 개발 도구
.serena/

# 배포 폴더 (ZIP만 버전 관리)
num-drweb-deploy/

# Electron 빌드 캐시
.electron-builder-cache/
dist/
node_modules/

# Cloudflare Workers 로컬 개발 상태 (server/)
.wrangler/

# wrangler 로컬 시크릿 — CERT_PRIVATE_KEY·ADMIN_TOKEN 이 들어가는 파일. 절대 커밋 금지
.dev.vars
.dev.vars.*

# 운영자가 키·토큰을 적어 두는 곳. 공개 저장소이므로 절대 커밋 금지
배포정보/
*.key
*.pem

# 가이드·테스트용 샘플 자료 (로컬 보관, 용량 큼)
guide sample/

# 유료 혜택 배포물 — 공개 저장소에 올리지 않는다 (R2 비공개 경로로 전달)
*.apk
*.xlsm

# 로컬 참고 자료 (원본 pptx·집계표 샘플, 저장소에 올리지 않는다)
사용가이드 참고/
외관집계표*.xlsx

# 안드로이드 서명 관련 자료 (인증서·지문). 저장소에 올리지 않는다
pic app/
*.der
*.jks
*.keystore
