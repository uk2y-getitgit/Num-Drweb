# NumDraw 유료화 — 작업 재개 인수인계

- 최종 갱신: 2026-08-11
- **재개할 때 이 문서부터 읽으면 됩니다.**

---

## 0. 한 줄 요약

Phase 1(라이선스 인증) 코드 완료. **빌드 검증 통과, Cloudflare 배포 진행 중.**
👉 **재개 지점은 9장 「지금 어디까지 왔나」 를 먼저 보세요.**

---

## 1. 재개 시 첫 명령

```
이 폴더의 유료화_작업재개_인수인계.md 기준으로 이어서 진행해줘
```

에이전트 7종(`.claude/agents/`)은 이미 등록되어 있어 이름으로 바로 호출됩니다.

---

## 2. 확정된 결정 (다시 묻지 말 것)

| 항목 | 결정값 |
|---|---|
| 결제 | 무통장입금 **수동 처리** (PG 연동 없음) |
| 인증 | **하이브리드** — 최초 1회 온라인 활성화 → 이후 오프라인 |
| 활성화 가능 PC | **2대** |
| 체험판 | **저장만 제한** — PDF 워터마크 + PDF 저장 1장 (기간 무제한).<br>불러오기·페이지 추가·편집은 **무제한** (2026-08-11 변경, 9-2장 참조) |
| 인프라 | **Cloudflare Workers + D1 + Pages** |
| 사업자등록 | 아직 없음, 등록 예정 |
| **가격** | ❌ **미정** — Phase 2(홈페이지) 착수 전 반드시 결정 필요 |

---

## 3. 완료된 것

### 3-1. 문서

- `프로그램_유료화_개발계획서.md` — 사용자 원본 계획서
- `docs/archive/유료화_호스팅_비교안.md` — 3안 비교 + 근거 출처
- `docs/archive/유료화_Phase1_구현계획.md` — 키/증서 포맷, D1 스키마, API 계약, 보안 검수 결과(12장)

### 3-2. 코드

- 클라이언트: `electron/license/` 7개 + `js/license.js` + `main.js`·`preload.js`·`app.js`·`pageManager.js`·`index.html`·`style.css`
- 서버: `server/` (Cloudflare Workers + D1)
- 회귀 스위트: `server/test/` — **신규**
- 배포 전 가드: `scripts/check-release-config.mjs` — **신규**

### 3-3. 보안 검수 지적사항 — 전건 종결

| ID | 내용 | 조치 |
|---|---|---|
| H-1 | 좌석 카운트 경쟁 조건 | 원자적 조건부 INSERT. **음성 대조로 검출력 확인 완료** |
| H-2 | 반납 후 재활성화 영구 차단 | 부분 유니크 인덱스. **음성 대조로 검출력 확인 완료** |
| M-1 | README가 revoke 효과를 반대로 안내 | `server/README.md` 5장 재작성 — 표로 실제 효과 명시 |
| M-2 | `/admin/*` rate limit·토큰 길이 검사 없음 | `ADMIN_TOKEN` 24자 미만 → 503, IP당 8회 실패 → 15분 잠금 |
| M-6 | 자리표시자 상태로 빌드 성공 | `prebuild` 가드가 빌드 중단 |
| L-2 | `reg.exe` CWD 하이재킹 | 절대경로 고정 |
| L-3 | 서명 대상 이중 허용 | 단일 경로. **서버 증서 통과 + JWS식 서명 거부 확인** |
| L-5 | `/api/activate` CORS `*` | CORS 헤더·OPTIONS 핸들러 전부 제거 |
| L-8 | `parsed.valid` 미확인 사용 | 확인 후에만 발급 |

미조치로 남긴 것(L-1·L-4·L-6·L-7)과 그 사유는 계획서 12장 표에 있다.

### 3-4. 검증 실행 결과

```
cd server && npm test        →  57건 전체 통과
node scripts/check-release-config.mjs
    자리표시자 상태          →  exit 1 (빌드 차단)  ✅
    키·URL 주입 상태         →  exit 0 (빌드 허용)  ✅
npm start                    →  정상 기동, 콘솔 에러 0건 (증서 없음 → 체험판 폴백)
```

> **음성 대조를 반드시 유지할 것.** 첫 버전 하네스는 구버전 버그 코드를 넣어도 57건 전부
> 통과했다 — 어댑터 `await`가 전부 마이크로태스크라 임계구역이 통째로 원자적으로 돌았기 때문.
> `setImmediate` 매크로태스크 경계를 넣어 고쳤다. 자세한 내용은 계획서 12-1장.
> **회귀 테스트를 손볼 때는 구버전 버그를 다시 주입해 실패하는지 먼저 확인한다.**

---

## 4. 다음 작업

| # | 작업 | 담당 | 비고 |
|---|---|---|---|
| 1 | 수정분 재검수 | `license-auditor` | M-2 잠금 로직·prebuild 가드가 새로 추가된 코드 |
| 2 | 빌드·설치·회귀 검증 | `release-qa` | NSIS `.exe` **한 번도 빌드해 본 적 없음** |
| 3 | 가격 결정 | 사용자 | Phase 2 착수 전 필수 |
| 4 | Phase 2 홈페이지 | `web-builder` | Cloudflare Pages |
| 5 | 약관·개인정보·환불정책 | `policy-writer` | |
| 6 | 사용가이드 콘텐츠 추출 | `docs-extractor` | |

### 사용자 작업 (배포 시)

👉 **`유료화_배포_따라하기.md`** 를 그대로 따라가면 된다.
비개발자 기준으로 1단계(빌드 확인) → 5단계(판매 시뮬레이션)까지 단계별 성공/실패 화면과
오류별 대처를 적어 놓았다. 요약하면:

1. **먼저 체험판 빌드부터** 확인 (`.exe`를 한 번도 만들어 본 적이 없어 여기서 막힐 수 있다)
2. Cloudflare 가입 → `npx wrangler login` → D1 생성 → `wrangler.toml`의 `database_id` 교체
3. `npm run genkeys` → 공개키는 `pubkey.js`, 개인키는 `wrangler secret put CERT_PRIVATE_KEY`
4. `wrangler secret put ADMIN_TOKEN` — **24자 이상** (미만이면 관리자 화면이 503)
5. `npm run deploy` → 주소를 `config.json`의 `activationUrl`에 `.../api/activate` 형태로
6. `npm run build` (가드가 자동 검사)
7. **판매 시뮬레이션** — 키 발급 → 활성화 → 재설치 → 2대 제한 → 반납 → 정지

운영 절차 전문은 `server/README.md`에 있다.

> `wrangler dev`는 이 PC에서 죽지만 **`wrangler login`·`d1 create`·`deploy`는 정상 동작**한다
> (CLI 자체는 멀쩡하고 `dev`가 띄우는 workerd 바이너리만 죽는다). 확인 완료.

### 배포 후 실환경 재검증 (생략 금지)

**서버 코드는 실제 workerd에서 한 번도 돌아본 적이 없다.** 회귀 스위트는 Node 어댑터
위에서만 돌았다. 계획서 11-3의 4개 시나리오 + H-1 동시 요청 확인을 실배포 환경에서
반드시 손으로 한 번 더 돌린다.

---

## 5. 알아둘 제약 (반복 삽질 방지)

| 제약 | 내용 |
|---|---|
| `wrangler dev` 불가 | 이 PC에서 workerd가 access violation(0xc0000005)으로 즉사. wrangler 3.x/4.x 동일, 경로·한글·괄호 무관. 대신 `cd server && npm test` (Node `node:sqlite` 어댑터) |
| `asar: false` | 설치 폴더 JS가 평문 노출·수정 가능. 한 줄 패치 방어 불가 — **인지된 한계**로 계획서 9장에 기록됨. 다시 지적할 필요 없음 |
| revoke의 실제 효과 | 이미 활성화된 PC는 **계속 동작한다**. 신규 활성화만 차단. `server/README.md` 5장 참조 |
| `.claude/` 미추적 | `.gitignore`에 있어 에이전트 정의 7종이 버전 관리에서 빠져 있다. 필요하면 `.claude/agents/`만 예외 처리 |
| `_exportPDF` | 3회 이상 반복 수정된 위험 함수. 수정 전 전체를 읽을 것 |
| 커밋 메시지 | 한국어 `기능:` `수정:` `문서:` `빌드:` `정리:` 접두어 필수 (훅이 검사) |
| 색상 | CSS 변수만 사용. 하드코딩하면 훅에 걸림 |

---

## 9. 지금 어디까지 왔나 (2026-08-11 기준) ⬅ 재개 지점

`유료화_배포_따라하기.md` 를 순서대로 진행 중이다.

### 9-1. 단계별 상태

| 단계 | 내용 | 상태 |
|---|---|---|
| 1-1 | `cd server && npm test` | ✅ **71건 전체 통과** |
| 1-2 | 시험용 빌드(`NUMDRAW_ALLOW_UNCONFIGURED_BUILD=1`) | ✅ 성공. 가드 경고 정상 출력, `dist\NumDraw Setup 1.2.0.exe` 생성 |
| 1-3 | 설치·실행 확인 | ✅ 사용자 확인 완료 |
| — | 체험판 제한 방식 변경 | ✅ 9-2장 참조 |
| 2-1 | Cloudflare 가입 | ✅ `chistonsop@gmail.com` |
| 2-2 | `npx wrangler login` | ✅ 계정 ID `eb2b1a83a9fca40abc3035c95310a843` |
| 2-3 | D1 생성 + `wrangler.toml` 반영 | ✅ `database_id = 6bdf1196-d87a-4a74-9a83-8a3794d3d471` (APAC) |
| 2-4 | `schema.sql --remote` 적용 | ✅ `licenses` · `activations` · `idx_activations_active_device` 확인 |
| 2-6 | `npm run deploy` | ✅ `https://numdraw-activation.numdraw.workers.dev` (Version `af90eb94`) |
| 2-7a | 서버 생존 확인 (`GET /`) | ✅ `NumDraw activation server OK` (200) |
| 2-5 | `npm run genkeys` → 시크릿 2종 등록 | ✅ `secret list` 에 `CERT_PRIVATE_KEY` · `ADMIN_TOKEN` 확인 |
| 2-7b | `/admin/issue` 로그인 | ✅ **503 → 401**(Basic 인증 요구)로 전환 확인. 실제 로그인은 5-1에서 |
| 3 | `pubkey.js` 공개키 · `config.json` 주소/문의처 주입 | ✅ 가드 `exit 0`. 공개키 `LJ4WoXQ7…MeirI`(Ed25519 32바이트 검증 완료), 문의처 `chistonsop@gmail.com`(임시 — 별도 주소 생기면 교체) |
| 4 | `npm run build` (판매용) | ✅ `dist\NumDraw Setup 1.2.0.exe` (2026-08-12 09:21). 패키지 내부 `pubkey.js`·`config.json` 주입 확인 |
| 5-1·5-2 | 키 발급 → 활성화 | ✅ D1 확인 `ND2-JS9M3-****-****-0T3A9` / 좌석 **1/2** / active |
| 5-4 | 2대 제한 | ⬜ **보류** — 다른 PC 확보 시. 첫 판매 전에는 반드시 |
| 5-5·5-6 | 좌석 반납 · 키 정지 | ⬜ 미확인 |

### 9-1-a. 해소된 위험 · 남은 위험

**해소됨 (5-2 활성화 성공으로 동시 증명)**

- 키쌍 일치 — 앱이 증서 검증을 통과했다 = `pubkey.js` 공개키와 등록된 개인키가 짝이다
- **workerd Ed25519 서명 동작** — 서버 코드가 실제 Cloudflare 런타임에서 처음으로 서명에 성공

**남은 위험**

| 위험 | 확인 방법 | 터지면 |
|---|---|---|
| 2대 제한 미검증 | 다른 PC에서 5-4 | 3대째가 뚫리면 **수익 직결**. 첫 판매 전 필수 |
| 좌석 반납·정지 미검증 | 5-5 · 5-6 | 반납 500 버그가 재발하면 PC 교체 대응 불가 |
| 체험판 저장 게이트 (9-2) | 인증 전 상태에서 여러 장 → PDF 저장 | 1장 제한이 안 걸리면 `js/app.js` `_exportPDF` 수정 + 재빌드 |

---

## 10. Phase 2 — 판매 홈페이지 (2026-08-12 착수)

| 항목 | 상태 |
|---|---|
| 랜딩 페이지 | ✅ `web/index.html` + `product.html`·`purchase.html`·`guide.html`·`terms.html` (2026-08-13 다중 페이지로 재구성) |
| 호스팅 | ✅ Cloudflare Pages 프로젝트 `numdraw` → **https://numdraw.pages.dev** |
| 배포 명령 | `cd server && npx wrangler pages deploy ../web --project-name numdraw --branch main --commit-dirty=true` |
| 설치파일 배포처 | ✅ R2 버킷 `numdraw-download` 개통·공개전환 완료 (2026-08-13). 공개 URL: `https://pub-a66add04be154b7e97486436051b6b2e.r2.dev/NumDraw-Setup-1.2.3.exe` |
| 가격 | 🟨 예시 가격 게시 (2026-08-13, `purchase.html`) — 체험판/월구독 5,990/연간 59,900/일회성 29,900/평생 109,900. **확정 아님**, 유료화 모델도 1회성→하이브리드(구독포함)로 변경됨 |
| 약관·개인정보·환불 | ✅ `web/terms.html` 게시 (2026-08-13). 사업자정보(골드랜드·313-14-63489·장경진) 반영, 주소·통신판매업신고번호는 미등록 |

### 10-1. 왜 설치파일을 Pages에 못 올리나 — ✅ 2026-08-13 해결됨

Pages는 **파일 하나당 25MB 제한**이고 설치파일은 **79MB**다. 그래서 R2(무료 10GB·
다운로드 트래픽 무료)에 올리고 페이지에서 링크한다. 실제로 실행한 명령:

```
npx wrangler r2 bucket create numdraw-download
npx wrangler r2 object put numdraw-download/NumDraw-Setup-1.2.3.exe --file "../dist/NumDraw Setup 1.2.3.exe" --remote
npx wrangler r2 bucket dev-url enable numdraw-download
```

공개 URL `https://pub-a66add04be154b7e97486436051b6b2e.r2.dev/NumDraw-Setup-1.2.3.exe` 를
`web/purchase.html` 의 `DOWNLOAD_URL` 에 넣어 버튼이 직접 다운로드로 전환됐다.
새 버전을 빌드하면 **같은 방식으로 새 파일명을 올리고 `DOWNLOAD_URL`만 갱신**하면 된다
(구버전 파일은 버킷에서 수동 삭제하지 않는 한 남아있음).

### 9-2. 체험판 제한 방식 변경 (2026-08-11)

**사용자 지적:** 체험판이 도면 **불러오기**부터 1장으로 막혀 있었다.
의도는 "여러 장 작업은 자유롭게 하되 **저장만** 1장으로 막아 유료 전환을 유도"하는 것.

게이트를 불러오기 → **PDF 저장**으로 옮겼다.

| 동작 | 변경 전 | 변경 후 |
|---|---|---|
| 도면 불러오기 / 페이지 추가 | 1장에서 차단 | **무제한** |
| 프로젝트(.numdraw) 불러오기 | 1장으로 잘림 | **전부 복원** |
| PDF 저장 | 전 페이지 저장 | **1장만** + 안내 + 인증 모달 |
| 워터마크 | 찍힘 | 그대로 |

수정 파일:
- `js/pageManager.js` — `_canAdd` · `_fromJSONTruncated` · `getTruncatedCount` 제거. `_maxPages()`는 저장 게이트용으로 유지
- `js/app.js` `_exportPDF()` — 저장 게이트 추가. **잘라낼 때 첫 페이지가 아니라 "현재 활성 페이지"부터** 담는다 (어느 페이지든 결과물을 확인해 보고 구매 판단하도록)
- `js/license.js` — 체험판 안내 문구를 "저장 제한"으로 수정
- `유료화_배포_따라하기.md` 1-3 · 5-2 확인표, `docs/archive/유료화_Phase1_구현계획.md` 체험판 정의 갱신

> ⚠ 이 변경은 **아직 실제 앱에서 확인 안 됐다.** 1-2 시험 빌드는 변경 **이전** 것이다.
> 4단계 빌드를 설치하면 인증 전까지 체험판 상태이므로, **5-2 직전에** 여러 장 불러와
> PDF 저장이 1장만 되는지 확인할 것.

### 9-3. 재개하면 사용자에게 요청할 것

2-5는 개인키를 다루므로 **에이전트가 대신 실행하면 안 된다.**
`npm run genkeys` 는 개인키를 stdout에 그대로 찍으므로 **`!` 프리픽스로도 실행시키지 말 것**
(대화 기록에 개인키가 남는다). 사용자가 **별도 터미널 창**에서 직접 해야 한다.

사용자에게 안내할 순서 (`server` 폴더에서):

1. `npm run genkeys` — **딱 한 번만** (두 번 돌리면 키쌍이 어긋나 5-2에서 증서 검증 실패)
2. `npx wrangler secret put CERT_PRIVATE_KEY` ← [개인키] 붙여넣기
3. `[Convert]::ToBase64String((1..32|%{Get-Random -Max 256}))` 로 토큰 생성 → 안전한 곳에 저장
4. `npx wrangler secret put ADMIN_TOKEN` ← 위 값 붙여넣기 (**24자 미만이면 관리자 화면 503**)
5. 그 터미널 창 닫기

그다음 사용자에게서 받아야 하는 값 **2가지**:

| 값 | 용도 | 비고 |
|---|---|---|
| **공개키** (43자) | `electron/license/pubkey.js` 의 `PUBLIC_KEY` | 유출돼도 안전 — 대화창에 붙여넣어도 됨 |
| **고객 문의용 이메일** | `electron/license/config.json` 의 `supportContact` | 미정. 개인 메일이 앱에 박히므로 별도 주소 권고함 |

이 둘을 받으면 **2-7b → 3 → 4 는 에이전트가 이어서 진행 가능**하다.
(`config.json` 의 `activationUrl` 에
`https://numdraw-activation.numdraw.workers.dev/api/activate` 를 넣고,
`node scripts/check-release-config.mjs` 로 확인한 뒤 `npm run build`)

> 2-6 배포는 2-5보다 먼저 끝냈다. Worker가 없는 상태에서 `wrangler secret put` 을 하면
> "새 Worker를 만들까요?" 를 먼저 묻기 때문이다. 시크릿은 요청 시점에 읽히므로
> (`src/routes/activate.js:37`, `src/util.js:135`) 지금 등록해도 **재배포가 필요 없다.**
