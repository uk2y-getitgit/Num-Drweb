# NumDraw 유료화 — 작업 재개 인수인계

- 중단 시각: 2026-08-07
- 중단 사유: 사용 한도. 한도 초기화 후 재개 예정
- **재개할 때 이 문서부터 읽으면 됩니다.**

---

## 0. 한 줄 요약

Phase 1(라이선스 인증) 코드는 클라이언트·서버 모두 **작성 완료 + 보안 검수 완료**. 검수에서 나온 **높음 2건은 수정 완료**. 남은 것은 **낮은 우선순위 수정 4건 + 검증 재실행 + 빌드 검증**이다.

> ⚠️ **코드 변경분이 아직 커밋되지 않았습니다.** 작업 폴더를 지우거나 `git checkout .` / `git reset --hard` 를 실행하면 전부 사라집니다. 재개 시 가장 먼저 WIP 커밋을 권합니다.

---

## 1. 재개 시 첫 명령

```
이 폴더의 유료화_작업재개_인수인계.md 기준으로 이어서 진행해줘
```

에이전트 7종(`.claude/agents/`)은 이미 등록되어 있어 이름으로 바로 호출됩니다.

---

## 2. 지금까지 완료된 것

### 2-1. 확정된 결정 (다시 묻지 말 것)

| 항목 | 결정값 |
|---|---|
| 결제 | 무통장입금 **수동 처리** (PG 연동 없음) |
| 인증 | **하이브리드** — 최초 1회 온라인 활성화 → 이후 오프라인 |
| 활성화 가능 PC | **2대** |
| 체험판 | **기능 제한** — PDF 워터마크 + 페이지 1장 (기간 무제한) |
| 인프라 | **Cloudflare Workers + D1 + Pages** |
| 사업자등록 | 아직 없음, 등록 예정 |
| **가격** | ❌ **미정** — Phase 2(홈페이지) 착수 전 반드시 결정 필요 |

### 2-2. 만들어진 산출물

**문서 (커밋 완료, 푸시됨 — `722c3df`)**
- `프로그램_유료화_개발계획서.md` — 사용자 원본 계획서, 결정사항 반영됨
- `유료화_호스팅_비교안.md` — 3안 비교 + 근거 출처
- `유료화_Phase1_구현계획.md` — 키/증서 포맷, D1 스키마, API 계약, 보안 검수 결과(12장)

**에이전트 7종 (`.claude/agents/`, `.gitignore`로 추적 안 됨)**

| 에이전트 | 모델 | 담당 |
|---|---|---|
| `license-core` | opus | 앱 내부 인증 모듈 |
| `license-auditor` | opus | 보안 검수 (읽기 전용) |
| `activation-server` | sonnet | 서버 + 발급 백오피스 |
| `web-builder` | sonnet | 판매 홈페이지 |
| `policy-writer` | sonnet | 약관·개인정보·환불 |
| `release-qa` | sonnet | 빌드·회귀 검증 |
| `docs-extractor` | haiku | 코드→단축키/메뉴 표 추출 |

**코드 (미커밋)**
- 클라이언트: `electron/license/` 7개 + `js/license.js` + `electron/main.js`·`preload.js`·`js/app.js`·`js/pageManager.js`·`index.html`·`css/style.css` 수정
- 서버: `server/` 12개 파일

---

## 3. 중단 시점의 정확한 상태 (파일 직접 확인함)

### ✅ 반영 완료

| ID | 내용 | 확인 근거 |
|---|---|---|
| **H-1** | 좌석 카운트 경쟁 조건 → **원자적 조건부 INSERT**로 교체 | `server/src/routes/activate.js:99-110` — `INSERT ... SELECT ... WHERE NOT EXISTS(...) AND (SELECT COUNT(*)...) < ?` + `meta.changes` 판정 |
| **H-2** | 반납 후 재활성화 영구 차단 → **부분 유니크 인덱스**로 교체 | `server/schema.sql:30-32` — `CREATE UNIQUE INDEX ... WHERE released_at IS NULL` |
| **L-2** | `reg.exe` CWD 하이재킹 → 절대경로 고정 | `electron/license/fingerprint.js:21-38` — `SystemRoot`/`windir`/`C:\Windows` 순 폴백 |
| **L-3** | 서명 대상 이중 허용 제거 | `electron/license/verify.js:59-73` — `ascii` 경로 사라지고 `payloadRaw` 단일 경로 |

### ❌ 미반영 — 여기서 중단됨

| ID | 심각도 | 내용 | 대상 파일 |
|---|---|---|---|
| **L-5** | 낮음 | `/api/activate`의 `access-control-allow-origin: *` 제거 (Electron 메인에서만 호출하므로 CORS 불필요) | `server/src/index.js:15` — **여전히 `'*'`** |
| **M-2** | 보통 | `/admin/*`에 rate limit·실패 잠금 없음. `ADMIN_TOKEN` 최소 길이 검사 없음 | `server/src/routes/admin.js`, `server/src/index.js:38-52` |
| **L-8** | 낮음 | `parseAndValidate(candidate).valid` 확인 없이 `.normalized` 사용 | `server/src/routes/admin.js:87-88` |
| **M-1** | 보통 | README가 "정지 버튼 = 즉시 모든 활성화 요청 거부"로 **사실과 다르게** 안내 | `server/README.md:141` — 미수정 |
| **M-6** | 보통 | 자리표시자 공개키·빈 URL 상태로 `npm run build`가 그냥 성공 → 배포 시 전 구매자 활성화 실패 | 루트 `package.json` — `prebuild` 스크립트 **없음** |

### ⚠️ 검증 미실행

두 에이전트 모두 **수정 후 검증에 도달하기 전에 중단**되었다. H-1·H-2 수정은 코드로만 확인했고 **실행 검증이 없다.**

돌려야 할 것:
1. **H-2 회귀** — 활성화 → 반납 → 같은 기기 재활성화가 **200**인가 (500이면 미수정)
2. **H-1 회귀** — 서로 다른 기기 5건 **동시**(`Promise.all`) → **정확히 2건만 성공, 3건 SEAT_LIMIT**
3. 기존 시나리오 재확인 — 신규 활성화 / 같은 기기 재요청 시 좌석 안 늘고 증서 재발급 / `INVALID_KEY` / `REVOKED` / admin 401
4. **L-3 수정 후** 서버 `cert.js`가 만든 증서가 여전히 클라이언트 검증을 통과하는가
5. `npm start` — 앱 정상 실행, 콘솔 에러 0건

> 이 PC에서는 `wrangler dev`가 workerd access violation(0xc0000005)으로 기동하지 않는다.
> 지금까지처럼 **라우트 코드를 수정 없이 Node `node:sqlite` D1 호환 어댑터 위에서** 돌리는 방식으로 검증한다.

---

## 4. 재개 후 작업 순서

| # | 작업 | 담당 에이전트 | 비고 |
|---|---|---|---|
| 1 | WIP 커밋 (유실 방지) | — | 가장 먼저 |
| 2 | L-5 · M-2 · L-8 · M-1 수정 + **H-1/H-2 회귀 검증** | `activation-server` | |
| 3 | M-6 `prebuild` 가드 추가 + **L-3 검증** + 앱 실행 확인 | `license-core` | 2번과 병렬 가능 (파일 안 겹침) |
| 4 | 수정분 재검수 | `license-auditor` | 읽기 전용 |
| 5 | 빌드·설치·회귀 검증 | `release-qa` | NSIS `.exe` 빌드 미검증 상태 |
| 6 | 전체 커밋·푸시 | — | |

2번과 3번은 `server/` vs `electron/`+`js/` 로 파일이 겹치지 않아 병렬 투입 가능하다. 단 **`유료화_Phase1_구현계획.md`는 한쪽만 수정**하게 해야 충돌이 없다.

---

## 5. 그 다음 (Phase 1 이후)

- [ ] **사용자 작업**: Cloudflare 계정 생성 → `wrangler login` → D1 생성 → `wrangler.toml`의 `database_id` 교체 → 시크릿 등록(`CERT_PRIVATE_KEY`, `ADMIN_TOKEN`) → `npm run deploy`
- [ ] **사용자 작업**: `cd server && npm run genkeys` → 공개키를 `electron/license/pubkey.js`에, 개인키를 Cloudflare Secret에
- [ ] **사용자 작업**: `electron/license/config.json`의 `activationUrl`·`supportContact` 입력
- [ ] **배포 후 실환경 재검증** — 계획서 11-3의 4개 시나리오 (생략 금지. 서버 코드가 실제 workerd에서 한 번도 안 돌아봤음)
- [ ] **가격 결정** ← Phase 2 착수 전 필수
- [ ] Phase 2 홈페이지 (`web-builder`) — 호스팅은 Cloudflare Pages로 확정
- [ ] 약관·개인정보처리방침·환불정책 (`policy-writer`)
- [ ] 사용가이드 콘텐츠 추출 (`docs-extractor`)

---

## 6. 알아둘 제약 (재개 시 반복 삽질 방지)

| 제약 | 내용 |
|---|---|
| `wrangler dev` 불가 | 이 PC에서 workerd가 access violation으로 즉사. wrangler 3.x/4.x 동일, 경로·한글·괄호 무관. Node `node:sqlite` 어댑터로 우회 중 |
| `asar: false` | 설치 폴더 JS가 평문 노출·수정 가능. 한 줄 패치 방어 불가 — **인지된 한계**로 계획서 9장에 기록됨. 다시 지적할 필요 없음 |
| `.claude/` 미추적 | `.gitignore`에 있어 에이전트 정의 7종이 버전 관리에서 빠져 있다. 필요하면 `.claude/agents/`만 예외 처리 |
| `_exportPDF` | 3회 이상 반복 수정된 위험 함수. 수정 전 전체를 읽을 것 |
| 커밋 메시지 | 한국어 `기능:` `수정:` `문서:` `빌드:` `정리:` 접두어 필수 (훅이 검사) |
| 색상 | CSS 변수만 사용. 하드코딩하면 훅에 걸림 |
