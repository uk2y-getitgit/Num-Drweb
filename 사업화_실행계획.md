# NumDraw 사업화 실행계획 — 배포 · 홍보 · 고객관리

- 작성일: 2026-08-19
- 이 문서의 자리: **지금부터 무엇을 어떤 순서로 하는가**를 정하는 상위 문서.
  - 로드맵(제품 기능) → `CLAUDE.md` §10
  - 유료화 전체 경위 → `프로그램_유료화_개발계획서.md`
  - 인프라 현재 상태·재개 지점 → `유료화_작업재개_인수인계.md`
  - 배포 손절차(비개발자용) → `유료화_배포_따라하기.md`
- 갱신 규칙: 항목을 끝내면 **그 자리에서 체크하고 완료 판정 근거를 한 줄 적는다.**
  근거 없는 체크는 하지 않는다 — 이 프로젝트는 "됐다고 적어둔 것"과 "실제로 되는 것"이
  이미 한 번 어긋났다(홈페이지 v1.2.4 · 설치파일 v1.2.3 불일치, §2-1).

---

## 0. 이번에 확정한 결정 (다시 묻지 말 것)

| 항목 | 결정 | 결정일 |
|---|---|---|
| 판매 개시 시점 | **날짜를 박지 않는다.** 게이트(§4)를 통과하면 연다 — 품질 우선 | 2026-08-19 |
| 결제 | **무통장입금 수동 유지 + 요금제 단순화.** PG·간편결제는 도입 *계획만* 세우고 트리거(§7)로 관리 | 2026-08-19 |
| 안드로이드 앱 | **APK 실물 있음**(`app-debug_Ver3.apk`) → 구매자에게 링크 제공. 단 디버그 빌드라 릴리스 서명본 판단 필요(§2-2-a) | 2026-08-19 |
| 외관조사 집계표 | **배포용 엑셀 양식 자체를 개선**한다 (앱 코드 변경 아님). 단 **현재 양식 실물이 없어 제작부터** 필요 | 2026-08-19 |
| 판로 | ① 안전진단·점검업체 직접 영업 ② 카페·단톡방 ③ 블로그·검색 유입 (영상은 제외) | 2026-08-19 |
| 에이전트 범위 | 릴리즈·배포 / 고객·라이선스 운영 / 품질·회귀테스트 / 홈페이지·홍보 콘텐츠 **4종** | 2026-08-19 |
| 고객 대장 | **구글 시트** (D1은 라이선스 원장, 시트는 거래·문의 원장) | 2026-08-19 |
| 출시 전 지출 | 도메인 구매 ○ / 통신판매업 신고 ○ / **코드서명 인증서 보류** | 2026-08-19 |
| 검증용 2번째 PC | 사무실 출근 시 사용 가능 → S1 게이트는 그때 일괄 처리 | 2026-08-19 |
| 투입 시간 | 주 5~15시간 → **에이전트가 초안·검증, 사람이 승인** 구조로 간다 | 2026-08-19 |

---

## 1. 현재 상태 (2026-08-19 실측)

### 1-1. 제품

| 항목 | 상태 |
|---|---|
| 앱 버전 | **v1.2.4** (제도 콘솔 UI · 다크/라이트 · 폰트 로컬 번들) — 2026-08-19 `main` 병합 완료 |
| 기능 | 외관조사망도 넘버링 / 사진 폴더 자동매칭 / 파일명 일괄변경 / 사진첩 6칸 PDF / 도곽 / 범례 / 장비시험망도 / 해치 도형 |
| 패키징 | Electron + electron-builder, `npm run build` → `dist\NumDraw Setup <ver>.exe` |
| 배포 가드 | `scripts/check-release-config.mjs` — 공개키·활성화 주소 미주입 시 빌드 중단 |

### 1-2. 판매 인프라

| 항목 | 상태 | 위치 |
|---|---|---|
| 활성화 서버 | 가동 중 | `https://numdraw-activation.numdraw.workers.dev` |
| 라이선스 DB | Cloudflare D1 (`licenses` / `activations`) | 계정 `chistonsop@gmail.com` |
| 관리자 화면 | `/admin/issue`(발급) · `/admin/list`(조회) · `/admin/revoke`(정지) · `/admin/release`(좌석 반납) | Basic 인증 |
| 서버 테스트 | `cd server && npm test` — 좌석 경쟁·반납·증서 검증·CSRF 포함 시나리오 스위트 | `server/test/run.mjs` |
| 홈페이지 | Cloudflare Pages 5페이지 (메인·제품소개·구매·가이드·약관) | `https://numdraw.pages.dev` |
| 설치파일 배포 | R2 버킷 `numdraw-download` 공개 URL | `pub-a66add04be154b7e97486436051b6b2e.r2.dev` |
| 인증 방식 | 최초 1회 온라인 활성화 → Ed25519 증서 오프라인 검증, **2대**까지 |
| 체험판 | 기간 무제한, **PDF 저장 1장 + 워터마크** |

### 1-3. 법무·사업자

| 항목 | 상태 |
|---|---|
| 사업자 | 골드랜드 · 313-14-63489 · 장경진 |
| 통신판매업 신고 | **미신고** — `web/terms.html` 자리표시자 |
| 사업장 주소·전화 | 미기재 |
| 약관·개인정보·환불 | 초안 게시됨, 신고번호·주소 확정 후 마감 필요 |
| 도메인 | 없음 (`numdraw.pages.dev` 사용 중) |
| 고객 문의처 | `chistonsop@gmail.com` (개인 메일, 앱 안에 박혀 있음 — 교체 시 재빌드 필요) |

---

## 2. 지금 어긋나 있는 것 (S0에서 전부 해소)

### 2-1. ~~홈페이지 v1.2.4 · 설치파일 v1.2.3 불일치~~ ✅ 해소 (2026-08-19)

홈페이지는 이미 **v1.2.4 새 UI 스크린샷과 버전 표기**로 나가 있는데,
R2에 올라간 설치파일은 `NumDraw-Setup-1.2.3.exe`(구 UI)다.
지금 체험판을 받는 사람은 **홈페이지에서 본 화면과 다른 프로그램**을 설치한다.
판매 전이라 금전 손해는 없지만, 열기 전에 반드시 맞춰야 한다.

> **해소됨 (2026-08-19)** — v1.2.4 설치파일을 R2 에 올리고 `web/purchase.html` 의
> `DOWNLOAD_URL`·표기를 갱신한 뒤 Pages 재배포. 세 값 일치 확인.
> 상세: `운영기록/배포/2026-08-19_v1.2.4.md`

### 2-2. 유료 혜택 공약 — 하나는 전달 경로가 없고, 하나는 실물이 없다 🔴

`web/purchase.html` 이 유료 플랜 공통 혜택으로 명시한 두 가지의 실태 (2026-08-19 확인):

| 혜택 | 실태 | 조치 |
|---|---|---|
| 사진번호 자동입력 안드로이드 앱 | **실물 있음** — `app-debug_Ver3.apk` (5.7MB). 다만 파일명이 말해 주듯 **디버그 빌드**다 | 전달 경로 마련 + 릴리스 서명본 여부 판단 (§2-2-a) |
| 외관조사 집계표 엑셀 양식 | **실물 없음** | 만들 때까지 혜택 목록에서 내리거나 "준비 중" 표기 |

지금 상태로 판매를 열면 "표시한 것을 주지 않는" 상태가 된다.

#### 2-2-a. APK 가 디버그 빌드라는 점

`app-debug` 는 안드로이드 스튜디오의 기본 디버그 산출물이다. 디버그 키스토어로 서명되어 있고
`debuggable` 플래그가 켜져 있어, 배포용으로는 다음 문제가 있다.

- 설치 시 경고가 더 뜨고, 기기에 따라 "출처를 알 수 없는 앱" 차단에 더 자주 걸린다
- 디버그 키는 매 빌드 환경마다 달라 **업데이트 설치가 서명 불일치로 실패**할 수 있다
- 최적화가 꺼져 있어 용량·성능이 릴리스 빌드보다 불리하다

유료 혜택으로 나가는 물건이므로 **릴리스 서명 APK 재빌드를 권한다.** 안드로이드 프로젝트 소스가
남아 있으면 어렵지 않고, 없으면 현 파일로 나가되 위 제약을 안내 문구에 적어야 한다.

#### 2-2-b. 안드로이드 — 플레이스토어 경로로 바뀌었고, 업로드 키를 잃어버렸다 🔴

**2026-08-20 조사 결과.** 위 2-2-a 는 "APK 를 직접 전달한다"는 전제로 쓰였는데, 실제 진행은
다르다. 2026-04-25 에 **플레이스토어 정식 등록**으로 방향이 정해졌고 거기까지 작업이 되어 있다.

| 항목 | 확인된 값 |
|---|---|
| 프로젝트 | `AI활용/pic-numinc` — **`Num-Drweb` 저장소 밖**, `github.com/uk2y-getitgit/pic-numinc` (master) |
| 패키지명 | `comJuLab.PicNuminc` — Play Console 등록값. 커밋 3개를 들여 맞춘 값이라 바꾸면 안 된다 |
| 저장소 버전 | `versionCode 2` · `versionName 1.1` |
| 스토어 상태 | 내부·비공개 테스트 트랙에 **버전이 올라가 있음** |
| 앱 서명 키 | 구글이 생성·보관 (`CN=Android, O=Google Inc.`, 4096bit RSA, 2026-04-25 발급, 2056년까지) |

**막힌 지점: 업로드 키 `.jks` 가 없다.** 그 키를 만든 PC 가 정리되었고, 이 PC 의 C·D·E 드라이브
전체와 `pic-numinc` 저장소 히스토리 어디에도 없다(`.gitignore` 에 `*.jks` 규칙이 없어 실수로
커밋됐을 가능성까지 확인했으나 없었다). 나온 것은 `debug.keystore` 하나뿐이라 쓸 수 없다.

**대조용 지문** — 다른 PC 나 백업에서 `.jks` 후보를 찾으면 아래 값과 맞는지 확인한다.
지문은 공개 정보라 기록해 두어도 위험하지 않다. 인증서 원본은 `pic app/` 에 있다(gitignore 처리됨).

```
업로드 키   SHA-1   68:47:1D:F5:E9:91:67:01:EA:10:E3:40:B9:63:26:5E:A2:47:04:2E
앱 서명 키  SHA-1   12:F3:20:FD:60:28:0C:D6:3C:EA:CE:49:8A:3E:F6:2D:5F:EF:94:A0
(참고) 이 PC 디버그 키  C3:69:AF:06:06:1C:0B:58:EC:CD:CD:16:C9:2B:74:99:68:59:C2:1D
```

대조 명령: `keytool -list -v -keystore <후보.jks>` 실행 후 SHA1 줄을 위 첫 줄과 비교한다.

**앱 자체는 안전하다.** 앱 서명 키는 구글이 갖고 있어 설치된 사용자에게 영향이 없다.
키를 못 찾으면 새 키를 만들어 `upload_certificate.pem` 을 내보내고 Play Console 에
**업로드 키 재설정**을 요청한다(승인 1~2 영업일). 절차는 `/android-release` 스킬 Phase 1-B.

#### 2-2-c. 외관집계표 — 배포용 양식 제작 완료 🟢

`외관집계표.xlsx` (33건: 옥상 5 · 지상2층 15 · 지상1층 13) 가 `js/photoBook.js` 파서와
호환됨을 확인했다. 시트명·데이터 시작행(4행)·P열 문구 모두 맞는다. P열에는 앱의
`_composeCaption` 과 같은 규칙이 엑셀 수식으로 들어 있다(신규 접두 · 치수 괄호 · 비고 괄호).

**배포용 양식 `외관집계표_양식.xlsx` 제작 완료 (2026-08-20).** 아래 항목을 모두 처리했다.
원본 작업본은 `pic app/외관집계표_원본백업.xlsx` 에 보관했다.

| 문제 | 처리 결과 |
|---|---|
| 🔴 외부 연결에 내부 정보 | `externalLinks` 파트 제거. 파일 어느 파트에도 사내 경로·타 용역명·담당자명이 남아 있지 않음(zip 전체 문자열 검사로 확인) |
| 🔴 이름 정의 파손 | 외부 참조·`#REF!` 이름 9개 삭제 후, 드롭다운용 이름 7개로 교체 |
| 🟡 드롭다운 없음 | `입력목록` 시트 신설 + B·C·D·K·M·N·O 7개 열에 목록 적용. 사용자가 목록을 늘릴 수 있다 |
| 🟡 범위 고정 | 집계 수식 51개의 범위를 `$4:$41` → `$4:$203` 으로 확장 |
| 🟡 **O열 미입력** | 33행 전부 비어 있어 보수물량이 0 으로 나오던 문제. 결함유형 참조표(R4:R16)를 참조하는 드롭다운을 걸고 안내에 명시 |
| 🟢 잔여물 | 프린터 설정 제거, 사용 범위 1,048,576행 → 실제 범위로 축소 (40.4KB → 36.0KB) |

**구성:** `사용안내` · `외관집계표` · `입력목록` 3개 시트. 실무 데이터 33건을 지우고 예시 3행만 남겼으며,
용역명은 입력 자리만 남겼다. `js/photoBook.js` 파서로 재검증해 `RF-01` · `RF-02` · `1F-01` 3건을
정상적으로 읽어 냄을 확인했다(시트가 3개로 늘어도 파서가 `외관집계표` 를 정확히 고른다).

**드롭다운 밖 입력 검증 (2026-08-20).** 사용자가 목록을 늘리고 이름 범위를 조정하지 않거나
아예 자유롭게 입력했을 때 사진첩 연동이 되는지 실파일로 확인했다.

- **된다.** 데이터 검증을 경고(warning) 수준으로 걸어 두어 목록 밖 값도 `[예]` 를 누르면 입력되고,
  `floorKeyOf` 의 표기 규칙만 맞으면 정상 연동된다. `지상10층` → `10F` → 사진 `1001` 로 처리됨을 확인.
- **규칙을 벗어나면 그 행은 통째로 빠진다.** `옥탑층` · `1F` · `기계실` 은 인식되지 않는다.
  층이 아닌 공간명은 `지상1층 기계실` 처럼 층을 앞에 붙이면 인식된다(뒤 글자는 무시).

**이 과정에서 잡은 오류:** 초판 입력목록에 넣었던 `옥탑층` 이 `floorKeyOf` 인식 대상이 아니었다.
드롭다운에서 고른 값인데도 사진첩에서 빠지는 상태였으므로 목록에서 제거하고, 지상 10층·지하 3층까지
넓혔다. 목록 15개 항목 전수 검사로 모두 인식됨을 확인했다.

**남은 위험 (앱 쪽 과제):** `loadSummary` 는 층키를 못 만든 행을 **조용히 건너뛴다.**
앱은 "집계표 N건을 읽었습니다"만 표시하므로, 사용자가 엑셀 행 수와 대조하지 않으면 누락을 모른다.
→ 앱 안 집계표 화면 개선의 1순위 항목으로 둔다.

<details><summary>원래 있던 문제 (기록용)</summary>



| 문제 | 내용 |
|---|---|
| 🔴 외부 연결에 내부 정보 | 사내 네트워크 드라이브에 있는 **다른 용역의 작업 폴더**를 참조하고 있었다. 경로 문자열에 발주 용역명·담당자 실명·대상 시설명이 그대로 들어간다. (구체적 경로는 공개 저장소에 적지 않는다 — 원본은 `pic app/외관집계표_원본백업.xlsx` 에서 확인) |
| 🔴 이름 정의 파손 | `조사위치`·`세부위치`·`손상내용`·`개소` 가 위 외부 파일을 가리키고, `비고`·`너비__4`·`연장__3` 은 `#REF!` |
| 🟡 드롭다운 없음 | S~X열에 결함유형·보수방안 목록이 있으나 데이터 검증은 0개. 사용자가 목록을 보고 손으로 입력해야 한다 |
| 🟡 범위 고정 | `SUMIF($O$4:$O$41,…)` 와 필터 `M1:M41`. 결함이 38건을 넘으면 조용히 빠진다 |
| 🟢 잔여물 | 특정 프린터 설정(`printerSettings1.bin`), 사용 범위가 1,048,576행까지 잡혀 있음 |

</details>

### 2-3. 요금제 5종 중 구독 2종이 인프라와 맞지 않는다 🟡

월 5,990 / 연 59,900 구독을 게시했지만 자동결제 인프라가 없다.
사람이 매월 청구·확인해야 하므로 거래가 늘수록 반드시 사고가 난다.
→ **결정(§0)대로 구독을 내리고 체험판 / 일회성 / 평생 3종으로 단순화**한다.

### 2-4. 회귀 테스트가 앱 쪽에 사실상 없다 🟡

`CLAUDE.md`가 "`test_bounds.mjs`가 매번 검사한다"고 적어 두었지만
**저장소에 그 파일이 없다.** 서버는 스위트가 있으나(`server/test/run.mjs`),
앱(넘버링·사진첩·PDF·라이선스 게이트)은 사람 눈 말고 검사 수단이 없다.
→ qa-runner 에이전트(§3-3)의 첫 과제로 삼는다. 문서의 거짓 기술도 같이 고친다.

### 2-5. 라이선스 미검증 4건 🔴 (판매 개시의 실질적 게이트)

| 미검증 | 터지면 |
|---|---|
| 2대 제한 (3대째 차단) | 한 키로 무제한 설치 = 수익 직결 |
| 좌석 반납 후 재활성화 | PC 교체 고객을 처리할 수 없음 |
| 키 정지(revoke) 후 실행 차단 | 환불·부정사용 대응 불가 |
| 오프라인 재실행 | 인터넷 없는 현장(지하·옥상)에서 앱이 안 열림 = 제품 정체성 붕괴 |

두 번째 PC가 있어야 하므로 **사무실 출근일에 §4 S1을 통째로 처리**한다.

---

## 3. 에이전트 체계 설계

### 3-0. 설계 원칙

주 5~15시간이라는 조건에서 유일하게 맞는 구조는
**"에이전트가 초안과 검증까지, 사람이 승인과 발송"** 이다.

| 사람만 하는 일 (에이전트에게 절대 위임 금지) | 이유 |
|---|---|
| `npm run genkeys` 및 개인키 취급 | 개인키가 대화 기록에 남으면 라이선스 체계 전체가 무너진다 |
| `wrangler secret put` (CERT_PRIVATE_KEY · ADMIN_TOKEN) | 위와 동일 |
| 실제 고객 메일 발송, 입금 확인 | 오발송·오인식의 책임이 사업자에게 있다 |
| 가격·약관·환불·사업자정보 문구의 최종 게시 | 표시광고·전자상거래법 책임 영역 |
| 통신판매업 신고, 도메인·인증서 결제 | 본인 인증·결제 필요 |

| 에이전트가 하는 일 |
|---|
| 절차의 모든 단계를 순서대로 밟고, **빠뜨린 단계를 잡아내는 것** |
| 초안 작성 (릴리스 노트, 홍보 글, 메일 문안, 제안서, 테스트 시나리오) |
| 사실 대조 (홈페이지 문구 ↔ 실제 코드 동작, 버전 표기 ↔ 실제 배포물) |
| 실행 후 결과 확인 및 보고 (빌드 성공 여부, 링크 응답 코드, 테스트 통과 수) |

**공통 금칙 (4종 에이전트 프롬프트에 모두 박는다)**

1. 코드에서 확인되지 않은 기능을 홈페이지·홍보물에 쓰지 않는다. (표시광고 위반 위험)
2. 고객 개인정보(이름·연락처·입금자명)를 저장소에 커밋하지 않는다. 구글 시트에만 둔다.
3. 시크릿(개인키·ADMIN_TOKEN·계좌번호)을 출력하거나 파일에 쓰지 않는다.
4. 되돌리기 어려운 외부 동작(R2 업로드, Pages 배포, 키 정지, 메일 발송)은 **직전에 사람 승인**을 받는다.
5. 확인 못 한 것은 "확인 못 함"이라고 보고한다. 통과했다고 적지 않는다.

### 3-1. `release-pilot` — 릴리즈·배포

> 지금 배포는 **손으로 8단계**다(빌드 → 가드 → 업로드 → URL 교체 → 버전표기 → Pages 배포 → 다운로드 확인 → 릴리스 노트).
> §2-1 불일치가 바로 이 단계 중 하나를 빠뜨려서 생긴 문제다. 첫 번째로 만들 에이전트다.

| 항목 | 내용 |
|---|---|
| 트리거 | "버전 올려서 배포해줘", 새 기능 병합 후 |
| 입력 | 새 버전 번호, 변경 요약 |
| 절차 | ① `package.json` 버전 ② `npm run checkrelease` 가드 ③ `npm run build` ④ **[승인]** R2 업로드 ⑤ `web/purchase.html` `DOWNLOAD_URL` ⑥ 홈페이지 버전 표기 일괄 갱신 ⑦ **[승인]** Pages 배포 ⑧ 공개 URL 응답·파일 크기 확인 ⑨ 릴리스 노트 초안 |
| 산출물 | 배포 기록 1건 (버전·일시·URL·확인 결과), 릴리스 노트 초안 |
| 도구 | Bash(빌드·wrangler), 파일 편집, 다운로드 URL 확인 |
| 완료 판정 | 홈페이지 표기 버전 = R2 파일 버전 = `package.json` 버전, 세 값이 같고 링크가 200 |
| 금지 | 가드(`checkrelease`) 실패를 우회하는 빌드. 승인 없는 업로드·배포 |

### 3-2. `license-desk` — 고객·라이선스 운영

| 항목 | 내용 |
|---|---|
| 트리거 | 입금 확인함 / 키 발급 / 좌석 반납 요청 / 환불·정지 / 고객 문의 도착 |
| 입력 | 구매자명·메일·플랜·입금 내역 (사람이 확인한 사실만) |
| 절차 | ① 시트 대장에 거래 행 추가 ② `/admin/issue` 발급 절차 안내·확인 ③ 발급 키를 **마스킹**해 기록 ④ 안내 메일 문안 작성 → **[승인]** 사람이 발송 ⑤ 활성화 여부 `/admin/list`로 확인 ⑥ 좌석 반납·정지는 사유를 시트에 남기고 처리 |
| 산출물 | 시트 행, 메일 문안, 처리 이력 |
| 도구 | 파일 편집(문안), 브라우저(관리자 화면 조회) |
| 완료 판정 | 시트 행 ↔ D1 `licenses` 행이 1:1로 맞는다 |
| 금지 | 전체 키 문자열을 대화·파일에 남기기. 대신 `ND2-JS9M3-****-****-0T3A9` 형태 |

**구글 시트 대장 열 구성 (초안 — S0에서 실제 시트로 만든다)**

- 거래 대장: `거래일 / 구매자 / 연락처 / 플랜 / 금액 / 입금확인일 / 라이선스키(마스킹) / 발급일 / 메일발송일 / 활성화 좌석수 / 상태(정상·정지·환불) / 비고`
- 문의 대장: `접수일 / 채널 / 문의자 / 내용 / 분류(사용법·오류·구매·환불) / 답변일 / 재발방지 조치`

### 3-3. `qa-runner` — 품질·회귀테스트

| 항목 | 내용 |
|---|---|
| 트리거 | 배포 직전, 기능 변경 후, 주간 점검 |
| 절차 | ① `cd server && npm test` ② 앱 자동 점검(작성 예정) ③ 사람이 눈으로 볼 수밖에 없는 항목은 **체크리스트를 출력**하고 결과를 받아 기록 |
| 첫 과제 | ⓐ `CLAUDE.md`의 존재하지 않는 `test_bounds.mjs` 기술 정정 ⓑ 실제 회귀 점검 스크립트 작성 — 작업범위 안내선이 PDF·사진첩 출력물에 새지 않는지, 사진첩 6칸 배치·둘째 줄 줄바꿈 규칙, 도면 배율을 바꿔도 넘버링이 같은 위치를 가리키는지 |
| 수동 체크리스트 | 넘버링 2클릭 → 사진 폴더 연결 → 자동매칭 → 파일명 변경 → 집계표 불러오기 → 사진첩 PDF → 도곽 → 장비모드 → 테마 전환 → 체험판 저장 게이트 |
| 완료 판정 | 배포마다 "통과 n건 / 실패 n건 / 확인 못 함 n건"이 기록으로 남는다 |
| 금지 | 돌리지 않은 항목을 통과로 적기 |

### 3-4. `market-writer` — 홈페이지·홍보 콘텐츠

| 항목 | 내용 |
|---|---|
| 트리거 | 기능 추가·가격 변경 후, 채널 게시글이 필요할 때 |
| 절차 | ① 변경된 기능을 **코드에서 확인** ② `web/` 5페이지 중 영향 받는 문구 찾기 ③ 수정안 제시 → **[승인]** ④ 채널별 초안: 카페·단톡(정보전달형, 광고 티 최소) / 블로그(검색어 중심 긴 글) / 직접영업(제안 메일 + 1장 요약) ⑤ `홍보_카피_모음.md` 갱신 |
| 검색어 후보 | 외관조사망도 넘버링 / 안전점검 결과보고서 사진첩 / 외관조사망도 프로그램 / 시설물 점검 사진 정리 |
| 완료 판정 | 홈페이지의 모든 기능 주장에 대해 "그 기능이 있는 코드 위치"를 댈 수 있다 |
| 금지 | 미개발 기능 언급, 경쟁사 비방, 실측 없는 수치("90% 단축" 등) |

### 3-5. 슬래시 스킬 (에이전트 호출용)

| 스킬 | 하는 일 |
|---|---|
| `/release <버전>` | release-pilot 절차 전체 |
| `/issue-key` | 입금 확인 → 발급 → 메일 문안 → 시트 기록 |
| `/qa` | 자동 점검 + 수동 체크리스트 출력·기록 |
| `/promo <채널>` | 채널별 게시글 초안 |
| `/weekly` | 주간 점검 — 미결 항목·문의 잔량·다음 주 할 일 정리 |

### 3-6. 구축 순서 (승인 지점)

1. `release-pilot` + `/release` → 곧바로 §2-1 불일치 해소에 투입
2. `qa-runner` + `/qa` → S1 검증 체크리스트를 이 에이전트가 만든다
3. `license-desk` + `/issue-key` → 시트 대장 확정 후
4. `market-writer` + `/promo` → 요금제 단순화 반영 시 함께

> **승인 대기**: 위 4종 + 스킬 5개를 `.claude/agents/` · `.claude/skills/`에 실제로 만드는 작업은
> 승인 후 진행한다. `.claude/`가 현재 `.gitignore` 대상이므로, 버전 관리하려면 예외 처리 여부도 함께 결정한다.

---

## 4. 추후작업 로드맵 — 게이트 방식

날짜를 박지 않는다. **앞 단계의 완료 판정을 통과해야 다음 단계로 간다.**

### S0 — 어긋난 것 맞추기 (막힌 것 없음 · 지금 바로)

| # | 작업 | 담당 | 완료 판정 |
|---|---|---|---|
| ~~S0-1~~ | ~~v1.2.4 판매용 빌드 → R2 업로드 → `DOWNLOAD_URL`·버전표기 갱신 → Pages 배포~~ ✅ 2026-08-19 | — | 세 값 일치 확인, R2 200 / Content-Length 81,241,627 = 로컬 파일. 기록: `운영기록/배포/2026-08-19_v1.2.4.md` |
| ~~S0-2~~ | ~~혜택 문구 정정~~ ✅ 2026-08-19 — 두 혜택을 **"(준비 중)"** 으로 내리고 "현재 구매 혜택에 포함되지 않습니다" 명시 | — | 홈페이지에 실물 없는 혜택 주장이 없음 |
| S0-2a | (대기) 안드로이드 — **경로가 바뀌었다: APK 직접 전달이 아니라 플레이스토어 등록.** 업로드 키 분실로 막힘 (§2-2-b) | 사람 | 업로드 키 확보 또는 재설정 승인 → 서명 AAB 업로드 |
| ~~S0-2b~~ | ~~외관조사 집계표 배포용 양식 제작~~ ✅ 2026-08-20 — `외관집계표_양식.xlsx` (§2-2-c) | — | 외부 연결·내부정보 제거 확인, 파서 재검증 3건 통과. **남은 일: 사람이 엑셀에서 열어 확인 → 홈페이지 "(준비 중)" 표기 복원** |
| ~~S0-3~~ | ~~요금제 3종 단순화 (구독 삭제)~~ ✅ 2026-08-19 | — | 월·연간 구독 카드 제거, 체험판/일회성/평생 3종. `web/` 전체에 구독 언급 0건 → `terms.html` 의 "1회성 구매, 구독 아님" 조항과 정합 |
| S0-4 | 구글 시트 대장 2종(거래·문의) 생성 — CSV 양식·안내 제공됨 (`.claude/skills/issue-key/assets/`) | 사람 | 시트 존재 + 열 구성 확정 |
| ~~S0-5~~ | ~~에이전트 4종 + 스킬 5개 구축~~ ✅ 2026-08-19 | — | `.claude/agents/` 4개 · `.claude/skills/` 5개 생성, 구조 검증 통과, `CLAUDE.md` §12 등록 |
| S0-6 | `CLAUDE.md` 허위 기술 정정 (`test_bounds.mjs`) | qa-runner | 문서와 저장소 상태 일치 |
| ~~S0-7~~ | ~~홈페이지 문구 사실관계 정정 + 로고 마크 앱과 통일~~ ✅ 2026-08-19 | — | 체험판 제한·표제란 입력방식 문구를 코드 기준으로 정정, 공개 페이지 `TODO` 0건, 5페이지 200 응답. 기록: `운영기록/배포/2026-08-19_web-문구정정.md` |

### S1 — 판매 전 필수 검증 게이트 🔴 (사무실 PC 확보일에 일괄)

| # | 검증 | 방법 | 완료 판정 |
|---|---|---|---|
| S1-1 | 2대 제한 | PC-A·PC-B 활성화 후 3대째(가상머신) 시도 | 3대째 거절 + `/admin/list` 좌석 2/2 |
| S1-2 | 좌석 반납 | `/admin/release` 후 같은 기기 재활성화 | 반납 성공(500 안 남) + 재활성화 성공 |
| S1-3 | 키 정지 | `/admin/revoke` 후 활성화 시도 | 정지된 키로 활성화 거절 |
| S1-4 | 오프라인 재실행 | 활성화된 PC의 네트워크를 끊고 실행 | 정상 실행 + 저장 가능 |
| S1-5 | 체험판 저장 게이트 | 인증 전 상태에서 여러 장 작업 → PDF 저장 | 1장만 저장 + 워터마크 + 안내 |
| S1-6 | 전체 흐름 시연 | (모의 입금) → 발급 → 메일 → 활성화 → 저장 | 6단계가 한 번에 이어짐, 소요시간 기록 |

> **이 게이트를 통과하기 전에는 유료 판매를 열지 않는다.** 체험판 배포는 계속해도 된다.

### S2 — 법적·신뢰 요건

| # | 작업 | 담당 | 완료 판정 |
|---|---|---|---|
| S2-1 | 도메인 구매 + Pages 커스텀 도메인 연결 | 사람 + release-pilot | 자체 도메인으로 5페이지 접속 |
| S2-2 | 고객 문의용 메일 주소 개설 (도메인 메일 권장) | 사람 | 수신 확인 |
| S2-3 | `electron/license/config.json` 문의처 교체 → **재빌드 필요** | release-pilot | 앱 안내 문구에 새 주소 |
| S2-4 | 통신판매업 신고 | 사람 | 신고번호 수령 |
| S2-5 | `terms.html` 자리표시자 마감 (신고번호·주소·전화·환불조건) | market-writer + 승인 | 자리표시자 0개 |
| S2-6 | 디지털 재화 청약철회 예외 고지 문구 확인 | market-writer + 승인 | 환불정책이 전자상거래법 고지 요건을 갖춤 |

### S3 — 베타 (3명 이상 확보 가능)

| # | 작업 | 완료 판정 |
|---|---|---|
| S3-1 | 베타 키 발급(3~5개) + 대장 등록 | 시트에 베타 구분 기록 |
| S3-2 | 베타 과제 배포 — "실제 현장 도면 1건으로 사진첩 PDF까지" | 3명이 과제를 받음 |
| S3-3 | 피드백 양식(막힌 지점·오류·요청) 수집 | 3명 × 1건 이상 회수 |
| S3-4 | 이슈 분류 → 치명/불편/요청 3단계, 치명은 즉시 수정 | 치명 이슈 0 상태로 마감 |
| S3-5 | 가이드·FAQ에 실제 질문 반영 | 베타에서 나온 질문이 FAQ에 있음 |

### S4 — 정식 오픈

| # | 작업 | 완료 판정 |
|---|---|---|
| S4-1 | 최종 가격 확정 | 3종 가격 결정·게시 |
| S4-2 | "정식 판매 준비 중" 배너 제거, 구매 흐름 활성화 | 구매 신청 → 계좌 안내가 실제로 동작 |
| S4-3 | 첫 판매 대응 리허설 (본인이 구매자 역할) | 신청부터 활성화까지 지연 없이 완주 |
| S4-4 | 채널 3종 동시 공지 (카페·단톡 / 블로그 / 직접영업 메일) | 각 채널 1건 이상 게시·발송 |
| S4-5 | 오픈 후 48시간 모니터링 | 문의 대응 지연 0건 |

### S5 — 오픈 후 운영 · 확장

| 트랙 | 항목 |
|---|---|
| 운영 | 주간 리듬(§5) 정착, CS 템플릿 축적, 매출·발급 현황 월 1회 정리 |
| 제품 | 외관조사 집계표 **엑셀 양식 개선**(확정 사항), 안드로이드 앱 배포 라인 정비, 사이드바 전체 페이지 통합 목록(Phase 3-c-1), 사진번호 입력 UX(3-c-2), 전차대조 사진첩 |
| 인프라 | PG·간편결제 도입 (트리거 §7), 코드서명 인증서 (트리거 §7), 자동 업데이트 |
| 판로 | 블로그 글 누적(월 1~2편), 업체 제안 리스트 관리, 재구매·추천 유도 |

---

## 5. 주간 운영 리듬 (주 5~15시간 기준)

| 시점 | 내용 | 시간 |
|---|---|---|
| 주 1회 (고정) | `/weekly` — 미결 항목·문의 잔량·다음 주 목표 정리 | 30분 |
| 주 1회 | 홍보 콘텐츠 1건 발행 (카페 or 블로그, 초안은 market-writer) | 1~2시간 |
| 수시 | 문의·구매 대응 (license-desk가 문안, 사람이 발송) | 건당 10분 |
| 격주 | 제품 개선 1건 (S5 제품 트랙에서 하나씩) | 3~6시간 |
| 배포 시 | `/qa` → `/release` | 1시간 |

> 주 5시간밖에 못 쓰는 주에는 **고정 항목(주간 점검 + 문의 대응)만** 지킨다.
> 홍보와 제품 개선은 미뤄도 되지만, 문의 대응이 밀리면 그때부터 신뢰가 깎인다.

---

## 6. 위험 등록부

| 위험 | 영향 | 대응 | 상태 |
|---|---|---|---|
| 2대 제한 미검증 | 수익 직결 | S1-1 | 🔴 미해소 |
| 오프라인 재실행 미검증 | 현장에서 앱이 안 열림 | S1-4 | 🔴 미해소 |
| 홈페이지 공약 미제공(APK 전달경로) | 표시광고 문제 | S0-2a — 업로드 키 재설정 후 스토어 게시 | 🟡 절반 해소 (엑셀 양식은 2026-08-20 완료) |
| ~~공개 저장소 히스토리에 APK 잔존~~ | 유료 혜택물 유출 | 2026-08-19 `filter-branch` 로 히스토리 재작성 + 강제 푸시. 로컬 blob 까지 제거(`.git` 204MB→6.4MB) | ✅ 해소 (단서: §6-1) |
| 배포 APK 가 디버그 빌드 | 설치 경고·업데이트 실패 | §2-2-a | 🟡 판단 대기 |
| 구독 수동 청구 | 운영 붕괴 | S0-3 (구독 삭제) | 🟡 처리 예정 |
| 통신판매업 미신고 상태 판매 | 법 위반 | S2-4 | 🟡 처리 예정 |
| 다운로드 시 브라우저 경고 | 설치 이탈 | 안내문 게시됨, 근본해결은 코드서명(보류) | 🟡 완화됨 |
| 앱 회귀 테스트 부재 | 팔린 뒤에 깨짐 | S0-6 · qa-runner | 🟡 처리 예정 |
| 개인 메일이 앱에 박혀 있음 | 문의 분리 불가 | S2-2 · S2-3 | 🟡 처리 예정 |
| 운영자 1인 · 주 5~15시간 | 대응 지연 | 에이전트 자동화 + 고정 리듬(§5) | 🟢 관리 중 |
| 첫 판매 시점 미정 | 동력 저하 | 게이트 통과 = 오픈으로 규칙화 | 🟢 관리 중 |

### 6-1. APK 히스토리 제거 — 남는 단서

2026-08-19, 계획서 커밋에 `git add -A` 로 딸려 들어간 APK 를 히스토리에서 지웠다.
`e3382c9^..HEAD` 6개 커밋을 `filter-branch --index-filter` 로 다시 쓰고 강제 푸시했으며,
재작성 전후 트리가 완전히 동일함(`git diff backup main` 무출력)을 확인한 뒤 백업 브랜치를 지웠다.

**다만 이것이 "세상에서 사라졌다"는 뜻은 아니다.**

- GitHub 은 도달 불가능해진 객체를 자체 GC 전까지 **커밋 SHA 직접 주소로 잠시 더 제공**할 수 있다.
  즉시 끊어야 하면 GitHub 지원에 삭제를 요청해야 한다.
- 노출 구간(약 1시간) 안에 누가 클론·포크했다면 그 사본은 회수할 수 없다.
- 파일은 작업 폴더에 그대로 있고 `.gitignore` 로 다시 들어가지 않게 막아 두었다 (`*.apk` · `*.xlsm`).

교훈: 배포물·고객자료가 섞이는 폴더에서 `git add -A` 를 쓰지 않는다. 경로를 지정해 담는다.

---

## 7. 전환 판단 기준 (미리 정해 두는 트리거)

| 전환 | 언제 검토하나 | 왜 이 기준인가 |
|---|---|---|
| PG·간편결제 도입 | 월 거래 **10건** 이상, 또는 무통장 안내 후 이탈이 눈에 띌 때 | 그 아래에서는 수동 처리 비용 < PG 수수료·심사 부담 |
| 구독제 재개 | 자동결제가 붙은 뒤에만 | 수동 구독은 반드시 사고 난다 |
| 코드서명 인증서 | 누적 판매 **30건** 이상, 또는 "경고 떠서 못 받았다" 문의 반복 | 연 20~50만원을 매출로 회수할 수 있을 때 |
| 유료 광고 | 자연 유입에서 전환 사례가 3건 이상 확인된 뒤 | 전환 경로가 검증되지 않은 광고는 돈만 태운다 |
| 인력·외주 | 문의 대응이 주 3시간을 넘길 때 | 그 시점이 1인 운영의 한계 신호 |

---

## 8. 사용자가 제공해야 할 자료 · 사용자만 할 수 있는 작업

**자료 (다음 작업 전에 필요)**

| 자료 | 쓰는 곳 |
|---|---|
| ~~안드로이드 APK 경로~~ ✅ 디버그본 있음. 단 배포 경로가 플레이스토어로 바뀌어 의미가 줄었다 | S0-2a |
| **안드로이드 업로드 키 `.jks`** ❌ **분실** — 4월 작업 PC 정리됨. 대조 지문은 §2-2-b | S0-2a |
| ~~집계표 엑셀 양식 원본~~ ✅ **확보** — `외관집계표.xlsx` (33건, 파서 호환 확인) | S0-2b |
| ~~구글 시트 방식~~ ✅ 새로 구성 — CSV 양식 제공됨, 사용자가 가져오기 | S0-4 |

**사용자만 할 수 있는 작업**

- 두 번째 PC에서의 S1 검증 (사무실 출근일)
- 도메인 구매, 통신판매업 신고, 판매용 계좌
- 고객 메일 실제 발송, 입금 확인
- 시크릿 관련 명령(`genkeys`, `wrangler secret put`) — **별도 터미널에서, 대화창에 붙여넣지 말 것**

---

## 9. 다음 한 걸음

> **2026-08-20 갱신 — 두 항목의 상태가 8/19 기록과 달라졌다**
> 1. **외관조사 집계표** (S0-2b) — 양식 실물을 확보했고 `js/photoBook.js` 파서와 호환됨을 확인했다.
>    남은 일은 "제작"이 아니라 **배포용으로 다듬기**다. 상세는 §2-2-c.
> 2. **안드로이드 앱** (S0-2a) — 전달 경로가 **플레이스토어 등록**으로 바뀌었다(2026-04-25 작업).
>    테스트 트랙에 버전이 올라가 있으나 **업로드 키를 잃어버려 후속 업로드가 막혔다.** 상세는 §2-2-b.
> 3. 둘 중 하나라도 실물이 생기면 `web/purchase.html` 의 "(준비 중)" 표기를 되돌린다.


1. ~~§8 자료 3건~~ ✅ 확인 완료 (APK 있음 / 집계표 양식 없음 / 시트 새로 구성)
2. ~~에이전트 4종 + 스킬 5개 구축~~ ✅ 2026-08-19 완료 — `/release` `/issue-key` `/qa` `/promo` `/weekly`
3. **`/release` 로 S0-1 실행** — v1.2.4 빌드 → R2 업로드 → 홈페이지 버전 일치 (지금 가능)
4. 판단 필요 2건 — APK 릴리스 서명본 재빌드 여부(§2-2-a), 공개 저장소 히스토리의 APK 정리 여부
5. 집계표 엑셀 양식 제작 착수 또는 혜택 문구에서 제외 (S0-2b)
6. 사무실 출근일이 정해지면 S1 게이트 일괄 처리
