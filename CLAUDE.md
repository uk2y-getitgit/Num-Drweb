# NumDraw — 프로젝트 개발 지침서

## 1. 프로젝트 목적

건축 시설물 안전점검자가 현장점검 후 결과보고서를 작성할 때 필요한
**도면 넘버링 작업**을 웹 브라우저에서 빠르고 간편하게 수행하기 위한 툴이다.

### 핵심 가치

- **간편함**: 클릭 2번으로 지시선 + 번호를 완성한다. 불필요한 조작을 최소화한다.
- **자율성**: 사용자가 색상, 선 두께, 시작 번호, 도구 종류를 자유롭게 선택할 수 있어야 한다.
- **시인성**: 어두운 배경 위에 밝은 UI, 충분한 대비와 명확한 레이블로 현장에서도 쉽게 읽힌다.
- **연결성**: 넘버링과 현장 사진을 연결해 Excel 사진첩 보고서로 출력하는 것이 최종 목표다.

---

## 2. 사용 흐름 (User Flow)

```
[도면 불러오기] → [넘버링 작업] → [사진 폴더 연결] → [자동매칭] → [Excel 보고서 출력]
     ↓                ↓
  PDF / 이미지      1클릭: 지시점
  드래그앤드롭      2클릭: 번호 위치
```

---

## 3. 기능 명세

### 3-1. 도면 불러오기
| 항목 | 명세 |
|------|------|
| 지원 포맷 | PDF (다중 페이지), JPG, PNG, GIF, BMP, WebP |
| 불러오기 방식 | 파일 선택 버튼, 드래그앤드롭 |
| PDF 렌더링 | PDF.js 사용, 페이지당 scale=2 (고해상도) |
| 다중 페이지 | 툴바에서 이전/다음 페이지 이동, 페이지 전환 시 넘버링 초기화 |

### 3-2. 지시선 그리기 (2클릭 방식)
| 클릭 순서 | 동작 |
|-----------|------|
| 1클릭 | P1 지정 — 도면의 실제 점검 위치 (화살표 끝 또는 점) |
| 2클릭 | P2 지정 — 번호 박스가 표시될 위치 |

- **미리보기**: P1 클릭 후 마우스 이동 시 점선으로 실시간 미리보기
- **취소**: ESC 키로 현재 그리기 취소

### 3-3. 지시점 도구 선택
| 도구 | 모양 | 설명 |
|------|------|------|
| 화살표 | ▶ → 번호박스 | 화살표 머리가 P1에 표시됨 |
| 점 | ● → 번호박스 | 채워진 원이 P1에 표시됨 |

- 사이드바에서 버튼 클릭으로 전환
- 도구 전환은 즉시 적용 (진행 중인 그리기에는 영향 없음)

### 3-4. 번호 박스
- P2 위치에 사각형(둥근 모서리) 배경 위에 흰색 숫자
- 번호는 추가 순서대로 자동 증가 (1, 2, 3 …)
- 시작 번호는 사이드바에서 변경 가능

### 3-5. 직교모드
- ON 상태: P1→P2 선이 수평 또는 수직으로만 고정됨 (X축/Y축 중 이동량이 큰 쪽)
- 토글 위치: 툴바 버튼, 사이드바 스위치 — 둘 다 동기화
- 단축키: **Q**

### 3-6. 자동정렬 (겹침 방지)
- 번호 박스(P2)끼리 겹치면 밀어내는 반복 알고리즘 적용
- 최대 100회 반복, 캔버스 경계 안에서만 이동
- 툴바 "자동정렬" 버튼으로 수동 실행

### 3-7. 줌 & 팬
| 조작 | 동작 |
|------|------|
| 마우스 휠 | 커서 위치 기준 확대/축소 |
| 중간 버튼(드래그) 또는 Alt+드래그 | 화면 이동(팬) |
| 우클릭 드래그 | 화면 이동(팬) |
| F5 | 도면 전체 화면 맞추기 |
| 툴바/플로팅 +/− 버튼 | 단계적 확대/축소 |

### 3-8. 넘버링 관리
- 사이드바 "넘버링" 탭에 목록 표시
- 항목 클릭 → 선택(하이라이트)
- ✕ 버튼 → 해당 번호 삭제 후 번호 재정렬
- Ctrl+Z → 마지막 번호 되돌리기
- 전체삭제 버튼 → 확인 후 모두 제거

### 3-9. 사진 폴더 연결
- File System Access API 사용 (Chrome / Edge 전용)
- 폴더 선택 시 이미지 파일만 필터링
- **파일명 접두어 필터**: 입력값으로 시작하는 파일만 표시 (예: "DSC")
- 파일명에서 뒤쪽 숫자 추출 → 넘버링 번호와 자동매칭
  - 예: `현장001.jpg` → 번호 1과 매칭, `DSC_042.jpg` → 번호 42와 매칭
- "번호 자동매칭" 버튼으로 재매칭 실행
- 매칭된 항목은 녹색으로 표시

### 3-10. 프로젝트 저장/불러오기
- JSON 형식으로 로컬에 저장 (모든 넘버링 좌표, 타입, 매칭 정보 포함)
- Ctrl+S 단축키 지원
- 저장 상태 표시: `● 저장됨` / `● 미저장`

---

## 4. 기술 스택

| 영역 | 기술 | 선택 이유 |
|------|------|-----------|
| 런타임 | 순수 HTML + CSS + Vanilla JS | 서버/빌드 불필요, 파일 더블클릭 실행 |
| 도면 렌더 | Canvas API | 픽셀 단위 제어, 고성능 |
| PDF | PDF.js (CDN) | 표준 PDF 렌더링 |
| 폴더 접근 | File System Access API | 로컬 폴더 직접 읽기 |
| Excel (예정) | SheetJS (xlsx) | 브라우저 내 Excel 생성 |

---

## 5. 파일 구조

```
Num-Drweb/
├── index.html            # 단일 진입점, 모든 UI 마크업
├── css/
│   └── style.css         # 전역 스타일, CSS 변수 기반 다크테마
├── js/
│   ├── annotation.js     # 넘버링 데이터 모델, 자동정렬 알고리즘
│   ├── canvas.js         # 도면 렌더링, 줌/팬, 지시선 그리기
│   ├── fileManager.js    # 로컬 폴더 접근, 사진 매칭 로직
│   ├── sidebar.js        # 사이드바 UI 렌더링 (순수 DOM)
│   └── app.js            # 메인 진입점, 모듈 간 이벤트 연결
├── lib/                  # 오프라인용 외부 라이브러리 (선택)
│   └── pdf.min.js
└── docs/                 # 기존 에이전트/문서 폴더 (건드리지 않음)
```

---

## 6. 모듈 책임 원칙

| 모듈 | 담당 | 담당하지 않는 것 |
|------|------|-----------------|
| `annotation.js` | 데이터(items 배열), 자동정렬 | 화면 그리기, DOM |
| `canvas.js` | Canvas 렌더링, 마우스 이벤트, 줌/팬 | 데이터 저장, 사이드바 |
| `fileManager.js` | 폴더/파일 I/O, 번호 추출 | UI 렌더 |
| `sidebar.js` | DOM 리스트 렌더링 | 비즈니스 로직 |
| `app.js` | 모듈 연결, 전역 이벤트 | 세부 로직 |

---

## 7. UX 원칙

1. **클릭 수 최소화**: 핵심 작업(넘버링 추가)은 2클릭으로 완결
2. **피드백 즉시**: 모든 조작 후 하단 상태 메시지 2초 표시
3. **실수 용납**: Ctrl+Z, 개별 삭제, 전체삭제 모두 제공
4. **키보드 병행**: Q(직교), F5(맞추기), Ctrl+Z, Ctrl+S, ESC 지원
5. **상태 가시성**: 저장 상태, 현재 넘버 수, 다음 번호를 항상 표시
6. **비파괴적 조작**: 번호 삭제 시 번호는 재정렬되나 좌표 정보는 유지

---

## 8. 디자인 시스템 (Figma + Linear 조합, 라이트 테마)

**테마:** 라이트 모드 (흰색 패널, 연회색 앱 배경)
**폰트:** Inter (Google Fonts) + 시스템 폰트 폴백
**강조색:** 인디고 보라 (#5B5BD6) — Figma 브랜드 컬러 기반

```css
--accent:       #5B5BD6   /* 인디고 보라 — 버튼, 선택, 포커스 */
--bg-app:       #EEEEF0   /* 앱 배경 */
--bg-panel:     #FFFFFF   /* 툴바·사이드바·모달 */
--bg-canvas:    #D8D8DC   /* 도면 캔버스 배경 */
--text-primary: #1A1A1F   /* 본문 */
--border:       #E2E2E8   /* 기본 보더 */
--success:      #2D9E6B   /* 매칭·저장 */
--danger:       #D93025   /* 삭제·오류 */
--warning:      #C7830A   /* 미저장 */
```

새 UI 컴포넌트 추가 시 반드시 위 변수를 사용한다. 하드코딩 색상 금지.

---

## 9. 개발 규칙

- **서버 불필요**: `index.html`을 직접 열어 실행 가능해야 한다
- **외부 의존성 최소화**: PDF.js 외 외부 라이브러리 추가 시 CDN + 로컬 fallback 병행
- **브라우저 호환**: Chrome / Edge 기준 개발 (File System Access API 필요)
- **주석**: 로직이 비직관적인 경우에만 한국어 한 줄 주석 작성
- **커밋 메시지**: `기능: 내용` 형식으로 한국어 작성

---

## 10. 향후 개발 로드맵

### Phase 1 — 현재 완료
- [x] PDF / 이미지 도면 불러오기
- [x] 2클릭 지시선 + 자동 넘버링
- [x] 화살표 / 점 도구 선택
- [x] 직교모드 (Q키, 사이드바 토글)
- [x] 번호 겹침 자동정렬
- [x] 로컬 사진 폴더 연결 및 자동매칭
- [x] 프로젝트 JSON 저장/불러오기
- [x] 삭제 버그 수정 (id 타입 불일치 → 정수 id + Number() 변환)
- [x] 이미지 영역 밖 클릭 차단
- [x] 직교모드 툴바 버튼 제거 (사이드바 + Q키만 유지)
- [x] 카테고리별 지시선 색상 (결함/보수/기타, 색상 편집 가능)
- [x] 글씨 색상 변경 (번호 박스 텍스트)
- [x] 넘버링 접두어 (1F-01, RF-02 형태, 2자리 패딩)
- [x] 지시선 꺾임 방식 4종 (직선/ㄱ자/ㄴ자/번개)

### Phase 2 — 완료
- [x] 페이지 메뉴바 — PDF 다중 페이지 독립 관리 (pageManager.js)
  - 좌측 페이지 패널, 썸네일 + 이름 편집, 페이지별 넘버링 독립 저장
- [x] 도면 도곽 생성 + 설정 모달 (titleBlock.js)
  - Project Title / Drawing Name / 축척 / 작성자 / 날짜 / 페이지
  - 도곽 ON/OFF 토글, 툴바 "도곽 설정" 버튼
- [x] PDF 저장 (jsPDF) — 도면 + 넘버링 + 도곽 합성, 다중 페이지 지원

### Phase 2-b — UI 개선 완료
- [x] 도곽 양식 샘플 기반 재설계 (PROJECT TITLE | 내용 | DRAWING NAME | 내용 | SCALE | 값)
- [x] 도곽 불필요 항목 제거 (작성자, 날짜, 페이지 삭제)
- [x] 페이지 추가(+) / 삭제(−) 버튼 (페이지 패널 헤더, 카드 호버 ✕)
- [x] 번호 글씨 색상 일괄 수정 (setConfig textColor → 모든 기존 항목 반영)
- [x] 화살표 방향 반전 토글 (지시선 방향 / 반대 방향)
- [x] 사이드바 num-badge 너비 auto — 접두어+번호 잘림 없음

### Phase 2-c — 빌드 전 수정 완료
- [x] JSON 저장/불러오기 기능 제거 (툴바·app.js·sidebar.js)
- [x] 넘버링·도곽 크기 축척 기능 (사이드바 슬라이더 0.5×~3.0×, Shift+휠 조절)
- [x] 드래그 드롭 시 기존 페이지 유지 + 뒤에 append (append 옵션 추가)

### Phase 2-d — 오류 수정 완료
- [x] 도곽 디자인 재설계: PROJECT TITLE·DRAWING NAME 중앙 정렬, 폰트 크기 blockH 비례
- [x] 넘버링 추가 시 도곽 사라짐 근본 해결 — afterRender 콜백 구조 도입
      (renderAnnotations 완료 후 항상 TitleBlock.render 자동 실행)
- [x] 넘버링 배율(scale) 전 페이지 동기화 — fromJSON 시 scale·tbScale 보존
- [x] 페이지 전환 후 넘버링 미표기 — onSwitch 먼저, Annotation 복원 나중

### Phase 2-e — 오류 수정 + 기능 보완 완료
- [x] 페이지 중복 생성 버그 근본 해결
      - 원인: dropzone이 canvas-wrap 내부에 있어 drop 이벤트 버블링 → _loadFile 2회 호출
      - 수정: wrap 단일 핸들러로 통합 (stopPropagation 대신 구조 정리)
      - 추가: PageManager.clearAll() — 이미지 불러오기 시 기존 페이지 완전 초기화
- [x] 편집화면 A4 고정 규격 (150dpi 기준: portrait 1240×1754 / landscape 1754×1240)
      - pageManager.js 내부에서 모든 이미지·PDF를 A4 캔버스로 자동 변환
      - 이미지는 상하좌우 여백(30px) + 하단 도곽 예약(80px) 안에 Contain 모드로 배치
- [x] 불러오는 이미지 해상도 자동 조정 (A4 내 여백 영역 기준, 중앙 배치)
- [x] 도곽 개별 항목 수정 기능
      - 열 너비 비율 (PROJECT TITLE / DRAWING NAME / SCALE) 슬라이더 조정
      - 라벨·내용 글씨 크기 개별 입력
      - 표제란 높이 슬라이더 조정

### Phase 3-a — 사진 파일명 변경 (테스트 버전 완료)
- [x] 사이드바 사진 탭 내 "파일명 변경" 섹션 추가
- [x] 매칭 번호 입력 → 해당 번호와 매칭된 사진 파일 자동 검색
- [x] 새 파일명 입력 → 원본 파일 읽기 → 새 파일 생성 → 원본 삭제
      (File System Access API: readwrite 모드, folderHandle.removeEntry 사용)
- [x] 변경 결과 실시간 표시 (성공: 초록, 실패: 빨강)

### Phase 3-b — 저장/불러오기 기능 완료
- [x] 자동 저장 (3초 debounce, LocalStorage + IndexedDB)
  - 변경사항 감지 시 자동 저장 (Annotation.init, 카테고리 색상, 도곽 설정, 접두어)
  - 저장 상태 실시간 표시 (미저장 / 저장 중 / ✓ 시각 저장됨)
- [x] 파일 저장/불러오기 (.numdraw 확장자)
  - 프로젝트 저장: 이미지 포함 완전한 JSON 다운로드
  - 프로젝트 불러오기: 앱 상태 완전 복원
- [x] 세션 복원 (앱 시작 시 confirm)
  - 미저장 상태에서 탭 닫기 경고 (beforeunload)
- [x] 모듈 및 이벤트 연결
  - storageManager.js 신규 (8개 함수)
  - app.js 수정 (_onSaveStatusChange, _restoreFromData, 버튼 이벤트)
  - index.html 수정 (저장/불러오기 버튼, 인디케이터)
  - style.css 수정 (인디케이터 스타일)

### Phase 3-c — 다음 예정 (UI 개선 3종)

#### 3-c-1. 사이드바 목록 전체 페이지 통합 표시
- 넘버링 탭: 현재 페이지 목록만 표시 → **전체 페이지** 넘버링 통합 목록으로 변경
  - 페이지 구분 헤더(예: "P1 — 외관조사망도") 사이에 해당 페이지 넘버링 나열
  - 페이지 이동 없이 전체 번호 현황 한눈에 파악
- 사진 탭: 폴더 내 사진을 전체 페이지 넘버링 기준으로 미리보기 표시

#### 3-c-2. 사이드바 사진번호 입력 UX 개선
- 우측 사이드바 전체 너비 확장 (220px → 260px 검토)
- `photo-num-input` 크기 확장 (현재 38px → 54px 이상)
- `type="number"` 숫자 상·하 스핀 버튼 CSS로 숨김
  (`-webkit-appearance: none` / `-moz-appearance: textfield`)
- **Tab 키 순차 이동**: 1F-01 사진번호 입력 후 Tab → 1F-02 사진번호 입력칸으로 포커스 이동
  - sidebar.js `renderNumList` 이벤트에서 keydown Tab 감지 후 다음 `.photo-num-input` focus()

#### 3-c-3. 도곽 설정 팝업 UX 개선
- 모달 오버레이 배경 제거 (`rgba` 어둡게 막는 overlay → 투명 또는 삭제)
- 팝업창 위치: 화면 우측 또는 좌측 끝에 고정 (캔버스 영역을 가리지 않도록)
  - `position: fixed; right: var(--sidebar-w); top: var(--toolbar-h);` 방식 검토
  - 또는 사이드바 위에 drawer 형태로 슬라이드 표시
- 팝업 띄운 상태에서 캔버스 완전히 상호작용 가능 (슬라이더 조절 시 즉시 캔버스 확인)
- 현재 `.modal-overlay { backdrop-filter: blur(2px) }` 제거 대상

### Phase 3-d — 장기 예정
- [ ] Electron 패키징 (Windows .exe 설치파일)
- [ ] CDN → 로컬 lib 파일 교체 (오프라인 지원)
- [ ] Excel 사진첩 보고서 내보내기 (SheetJS)
  - 넘버링 번호 + 매칭 사진 + 위치 정보 포함
  - 사진첩 양식 자동 생성 (번호/사진/설명 레이아웃)

### Phase 4 — 온라인 확장 (장기)
- [ ] 프로젝트 클라우드 동기화
- [ ] 팀 공유 기능
- [ ] 모바일/태블릿 터치 지원

---

## 11. 알려진 제약사항

| 제약 | 원인 | 대응 |
|------|------|------|
| 사진 폴더 선택 불가 | Firefox 미지원 | Chrome/Edge 사용 안내 |
| PDF CDN 의존 | 오프라인 시 PDF 불가 | lib/pdf.min.js 로컬 복사본 안내 |
| 페이지 전환 시 넘버링 초기화 | 현재 설계 | 추후 페이지별 저장 구조로 개선 예정 |
