# NumDraw — 사진첩 생성 기능 개발계획 (Phase 3-e)

> 작성일: 2026-08-13
> 목표: 현재 별도 엑셀(`부록. 사진첩 - 메뉴.xlsm`)로 하던 **사진 삽입 + 사진첩 출력**을
> NumDraw 프로그램 안에서 끝내고 **PDF로 바로 출력**한다.

---

## 1. 배경

현재 작업 흐름은 두 개의 도구로 쪼개져 있다.

```
[NumDraw]                          [엑셀 xlsm]
도면 넘버링 → 사진폴더 연결       사진첩 시트에 사진 삽입
→ 자동매칭 → 파일명 일괄변경  ──▶  → 문구(결함내용) 매칭 → 인쇄/PDF
```

NumDraw가 이미 **번호 ↔ 사진파일 대응관계**를 전부 알고 있으므로,
엑셀로 넘길 이유가 없다. 사진첩 생성까지 앱 안에서 끝내는 것이 이번 목표다.

---

## 2. 현행 엑셀 매크로 분석 결과 (실측)

`부록. 사진첩 - 메뉴.xlsm` 을 해체해 VBA 7개 모듈 + 시트 구조를 전부 확인했다.

### 2-1. 워크북 구성

| 시트 | 역할 |
|------|------|
| `외관집계표` | 결함 원장 (번호·조사위치·세부위치·손상내용·규격). 문구의 원천 |
| `외관조사 사진첩` | 금차 사진첩 (페이지당 6칸) |
| `전차대조 사진첩` | 전차/금차 비교 사진첩 (페이지당 3건 × 2장) |

리본 탭 `사진첩 도구` 에 버튼 5개 (`customUI14.xml`):
사진매칭(폴더선택) / 리스트 매칭 / 전차사진 정렬 / 대각선 표시.

### 2-2. 외관조사 사진첩 — 페이지 격자 (실측값)

```
용지: A4 세로 / 인쇄배율 89% / 여백 좌우 13mm, 상하 4mm
행높이: 18.95pt 균일 (5행 표제 30pt)

5행    ┌──────────────────────────────────────┐  A5:N5 병합 = 용역명
       ├────────────┬────────────┬────────────┤
6~19행 │ A: 1F-01   │  B:G 사진  │ H: 1F-02   │ I:N 사진
       │            │  (12행)    │            │
       │            ├────────────┤            ├────────────
       │            │ B:G 문구   │            │ I:N 문구  (2행)
20~33  │            (같은 구조 반복)          │
34~47  │            (같은 구조 반복)          │
       └──────────────────────────────────────┘
다음 페이지 = +45행 (6 → 51 → 96 …)
```

- **페이지당 6칸** (3행 × 2열)
- 사진칸 실측 ≈ **231pt × 227pt (거의 정사각형)**
- 사진은 `LockAspectRatio = msoFalse` → **비율 무시하고 칸에 꽉 늘려 넣음**, 여백 1mm(2.835pt)

### 2-3. 사진 매칭 로직 (`Module3.사진번호_매칭_최종`)

1. 폴더 선택 (`msoFileDialogFolderPicker`)
2. 6행부터 12행씩 건너뛰며 순회, A열/B열에 `F-` 또는 `W-` 가 있는 행만 처리
3. 번호 → 파일명 변환은 **시트 수식(Q/R열)** 이 담당
   ```
   Q6 = IF(LEFT(A6,1)="W", SUBSTITUTE(A6,"-",""),
            SUBSTITUTE(LEFT(A6,FIND("F",A6)-1) & RIGHT(A6,2), "-", ""))
   → 1F-01 → 101,  2F-01 → 201,  W-01 → W01
   ```
4. `Dir(폴더 & 이름 & ".*")` — 확장자 무관 검색
5. 병합셀 영역에 `AddPicture` 후 칸 크기로 강제 리사이즈

> **NumDraw의 `FileManager.extractPhotoName()` 이 이미 동일한 규칙을 구현하고 있다.**
> (`1F-01→101`, `B1F-01→B101`, `RF-01→R01`) — 그대로 재사용 가능.

### 2-4. 문구 매칭 로직 (`Module5.MatchDefectsFinal_Fixed`)

1. 사진첩의 번호칸(A/H) 값 `1F-01` 을 `층키(1F)` + `번호(1)` 로 분해
2. `외관집계표` 를 4행부터 훑으며 **층키 대조 + 번호 일치** 검색
   ```vba
   Function IsMatchFloorFixed(key, data)
     W…  → data 에 "외부" 포함
     R…  → data 에 "지붕" 또는 "옥상" 포함
     B#F → data 에 "지하" + 숫자 포함
     #F  → data 에 "지상" + 숫자 포함
   ```
3. 일치하면 집계표의 **조합 문구**를 사진첩 문구칸에 기입
4. 문구에 `(보수완료)` 가 있으면 그 6글자만 **파란색 + 굵게**

집계표의 조합 문구 수식:
```
= IF(비고에 "신규" 포함, "(신규) ", "")
  & 조사위치 & " " & 세부위치 & " " & 손상내용
  & IF(폭/길이/너비 있으면 " (폭x길이)")
→ "지상1층 벽체 수평 및 수직균열 (0.2x2)"
```

### 2-5. 전차대조 사진첩

| 매크로 | 동작 |
|--------|------|
| `Module1` | 선택한 사진을 현재 병합셀 크기에 맞춰 정렬 (수동 캡처 붙여넣기용) |
| `Module6` | 금차 문구(H열) 매칭 — 외관과 동일 로직 |
| `Module4` | 전차 사진 없고 금차 사진 있으면 → 좌측 문구 삭제 + **좌측 칸에 대각선(신규 결함 표시)**<br>금차 문구 없고 전차 문구 있으면 → 좌측 문구를 우측으로 복사(변화 없는 결함) |

구조: 페이지당 3건, `B:G` = 전차분 사진, `H:M` = 금차분 사진.

---

## 3. NumDraw 현재 자산 (재사용 가능)

| 자산 | 위치 | 사진첩에서의 역할 |
|------|------|------------------|
| 번호 데이터 (`num`, `prefix`, `note`) | `annotation.js` | 사진첩 번호 라벨 + 문구 |
| 사진 폴더 핸들 · `photos[]` | `fileManager.js:12` | 사진 원본 읽기 |
| `extractPhotoName()` | `fileManager.js:45` | 번호 → 파일명 규칙 (엑셀 Q/R 수식과 동일) |
| `autoMatch()` / `photoName` | `fileManager.js:62` | 번호 ↔ 사진 대응표 |
| 페이지별 `prefix` | `pageManager.js:163` | 층 구분 (`1F`, `RF`, `W`) |
| jsPDF + 캔버스→PDF 파이프라인 | `app.js:1133 _exportPDF` | 사진첩 PDF 출력에 그대로 재사용 |
| 체험판 게이트 / 워터마크 | `app.js:1144`, `license.js` | 사진첩 PDF에도 동일 적용 |

**결론: 새로 만들어야 할 것은 "레이아웃 렌더러" 하나뿐이다.** 데이터는 이미 다 있다.

---

## 4. 설계

### 4-1. 신규 모듈 `js/photoBook.js`

기존 모듈 책임 원칙(CLAUDE.md §6)을 따라 **데이터 조립 + 캔버스 렌더링**만 담당한다.
DOM은 건드리지 않고, PDF 저장은 `app.js` 가 호출한다.

```js
const PhotoBook = (() => {
  loadSummary(file)       // 외관집계표 파싱 → 행 순서 그대로 보관
  collectEntries()        // 집계표 순서로 항목 조립 + 넘버링·사진 대조
  validate()              // 미매칭 검사 → { noPhoto[], noNumbering[], orphanNumbering[] }
  layout()                // A4 6칸 격자 좌표 계산
  renderPage(entries, n)  // 캔버스 1장 그리기 (표제 + 격자 + 사진 + 문구)
  buildAll(progress)      // 전체 캔버스 배열 반환
})();
```

### 4-2. 데이터 흐름 — **외관집계표가 순서의 기준**

사진첩 배치 순서는 도면 페이지 순서가 아니라 **집계표 행 순서**를 그대로 따른다
(실무 순서: 옥상층 → 지상 → 지하 → 외부). 층이 바뀌어도 페이지를 가르지 않고
6칸을 연속으로 채운다. (예: 1F 4장 + 2F 2장 = 한 장에 구성)

```
외관집계표 (.xlsx)  ← 순서의 기준
   │  A열 번호 · B열 조사위치 · P열 문구
   ▼
 각 행마다:
   조사위치 "지상1층" ─(층키 역변환)─▶ 층키 "1F" + 번호 1 ─▶ 라벨 "1F-01"
        │
        ├─▶ NumDraw 넘버링 대조     : 해당 라벨이 도면에 존재하는가?
        ├─▶ FileManager.photos 조회 : extractPhotoName("1F-01") = "101" → 101.jpg
        └─▶ 문구                    : 집계표 P열 (없으면 item.note 로 보정)
        ▼
   [{ label:'1F-01', img:Bitmap|null, caption:'지상1층 벽체 수평균열 (0.2x2)' }, …]
        ▼
   renderPage() × N  →  jsPDF (기존 _exportPDF 와 동일한 캔버스→JPEG→PDF 경로)
```

**층키 역변환** (`IsMatchFloorFixed` 의 역방향):

| 집계표 조사위치 | 층키 | 파일명 |
|---|---|---|
| 옥상층 / 지붕층 | `RF` | `R01` |
| 지상1층 | `1F` | `101` |
| 지상10층 | `10F` | `1005` |
| 지하1층 | `B1F` | `B101` |
| 외부 | `W` | `W01` |

같은 층 안에서의 번호는 집계표 A열 번호를 그대로 쓴다.

### 4-3. 레이아웃 (엑셀 격자 재현)

A4 세로 1240×1754px(150dpi) 캔버스에 엑셀과 동일 비율로 그린다.

```
┌─────────────────────────────────────┐ ← 상단 여백 4mm
│      용 역 명 (도곽 Project Title)   │
├─────────────────┬───────────────────┤
│ 1F-01 │ 사진    │ 1F-02 │ 사진      │  사진칸 비율 1:0.98 (엑셀 실측)
│       ├─────────┤       ├───────────┤
│       │ 문구    │       │ 문구      │  문구칸 = 사진칸 높이의 1/6
├─────────────────┼───────────────────┤
│ 1F-03 │ …       │ 1F-04 │ …         │
├─────────────────┼───────────────────┤
│ 2F-01 │ …       │ 2F-02 │ …         │  층이 바뀌어도 페이지를 가르지 않음
└─────────────────┴───────────────────┘
```

- **6칸/페이지 고정** (옵션 없음)
- 사진 맞춤: **늘리기** — 엑셀 매크로와 동일 (`LockAspectRatio=False` 재현)
- 표제(용역명): **도곽 설정의 Project Title 연동** — 사진첩 전용 입력칸을 따로 두지 않는다
- 색상은 CSS 변수 대신 캔버스 직접 지정 — 인쇄 기준 검정 테두리 + 흰 배경 고정

### 4-4. 문구(캡션) — 외관집계표 **P열** (확정)

| 우선순위 | 소스 | 설명 |
|---------|------|------|
| 1 | **집계표 P열** | `Module5` 와 동일한 열. 조합 문구가 들어 있다 |
| 2 | `item.note` | 집계표에 없는 번호를 앱에서 직접 보정 (예외 처리용) |

`(보수완료)` 파란색·굵게, `(신규)` 접두어도 캔버스 텍스트 렌더에서 그대로 재현한다.

### 4-5. 미매칭 검증 (확정)

매칭 실패는 **작업 오류 신호**로 취급한다. 조용히 넘기지 않는다.

| 상황 | 사진첩 표시 | 알림 |
|------|------------|------|
| 번호는 있는데 사진 파일이 없음 | 칸에 **"사진 없음"** 표기 (칸은 유지) | 팝업에 번호 나열 |
| 집계표에 있는데 도면 넘버링에 없음 | 칸 생성 (사진 없음) | 팝업에 별도 구분 표기 |
| 도면에 찍었는데 집계표에 없음 | 사진첩에서 제외 | 팝업에 별도 구분 표기 |

PDF 저장 직전에 검증 결과를 모아 한 번에 띄운다.

```
⚠ 매칭되지 않은 번호가 있습니다

사진 없음 (3건)          1F-04, 2F-07, RF-02
집계표에만 있음 (1건)     B1F-03
도면에만 있음 (2건)       W-05, W-06

[ 그대로 생성 ]  [ 취소하고 확인 ]
```

### 4-6. UI

사이드바 **사진 탭 하단**에 섹션 추가 (`index.html:428` 직전).

```
┌ 사진첩 ────────────────────┐
│ [ 외관집계표 불러오기(.xlsx)]│
│  ✓ 42건 · 옥상~외부         │
│ [   사진첩 미리보기   ]     │
│ [   사진첩 PDF 저장   ]     │
└────────────────────────────┘
```

미리보기는 도곽 설정 팝업과 동일한 모달 패턴을 재사용하고,
Phase 3-c-3 방침(오버레이 없는 우측 고정 패널)에 맞춘다.

---

## 5. 단계별 구현 계획

| 단계 | 내용 | 산출물 | 예상 |
|------|------|--------|------|
| **3-e-1** | 집계표 가져오기 — SheetJS 도입, P열 문구 추출, 층키 역변환, 집계표 행 순서 보존 | `photoBook.js`, `lib/xlsx.min.js` | 1.5일 |
| **3-e-2** | 대조·검증 — 집계표 ↔ 넘버링 ↔ 사진 3자 대조, 미매칭 3종 분류, 사이드바 썸네일 검수 | `photoBook.js`, `sidebar.js` | 1일 |
| **3-e-3** | 사진첩 렌더 — A4 6칸 격자 + 캔버스 렌더 + PDF 저장 (늘리기 방식) | `photoBook.js`, `app.js`, `index.html` | 2일 |
| **3-e-4** | 마감 — Project Title 표제, `(보수완료)`/`(신규)` 서식, "사진 없음" 표기, 미매칭 팝업, 리샘플, 진행률, 체험판 게이트 | 마감 | 1.5일 |
| **보류** | 전차대조 사진첩 — 전차분 폴더 추가 연결, 대각선(신규 결함) 표기 | 다음 범위 | 2일 |

**3-e-1 ~ 3-e-4 (총 6일) 완료 시점에 외관조사 사진첩은 엑셀 없이 완결된다.**
출력은 PDF 단일 형식으로 하고, xlsx 출력은 로드맵에서 제외한다.

---

## 6. 기술 리스크 및 대응

| 리스크 | 내용 | 대응 |
|--------|------|------|
| 메모리 | 사진 300장 × 4MB 를 한 번에 올리면 브라우저가 죽는다 | 페이지 단위 스트리밍 렌더 후 즉시 `bitmap.close()`, 장변 1600px·JPEG 0.8 로 리샘플 |
| PDF 용량 | 무손실이면 수백 MB | 칸 크기(≈600px)에 맞춰 축소 후 삽입 → 6칸 페이지당 약 400KB |
| EXIF 회전 | 현장 세로 촬영 사진이 눕는다 | `createImageBitmap(file, { imageOrientation:'from-image' })` |
| 폴더 권한 | 세션 복원 시 폴더 핸들이 살아나지 않음 | 사진첩 생성 시 폴더 미선택이면 재선택 안내 (기존 동작과 동일) |
| 체험판 | 사진첩 PDF가 게이트 우회 통로가 될 수 있음 | `_exportPDF` 와 동일하게 `getMaxPages()` · `hasWatermark()` 적용 |
| SheetJS | CDN 의존 추가 | CLAUDE.md §9에 따라 `lib/xlsx.min.js` 로컬 fallback 동시 배치 |

---

## 7. 확정 사항 (전부 확정 — 착수 가능)

| 항목 | 결정 |
|------|------|
| 범위 | **외관조사 사진첩만**. 전차대조는 다음 범위로 보류 |
| 문구 소스 | **외관집계표 P열**. 앱 내 메모(`item.note`)는 예외 보정용 |
| 배치 순서 | **집계표 행 순서 그대로** (옥상층 → 지상 → 지하 → 외부) |
| 페이지 분할 | 층이 바뀌어도 **가르지 않고 이어서** 채움 (1F 4장 + 2F 2장 = 한 장) |
| 칸 수 | **6칸 고정** (3행 × 2열), 옵션 없음 |
| 사진 맞춤 | **늘리기** (현행 엑셀 매크로와 동일한 결과) |
| 표제 | **도곽 설정의 Project Title 연동** |
| 미매칭 | **"사진 없음" 표기 + 저장 직전 경고 팝업** (오류 신호로 취급) |
| 출력 형식 | **PDF 단일**. xlsx 사진첩 출력은 개발하지 않음 |

---

## 8. 착수 시 확인할 것

사용자 확인이 아니라 **구현 첫날 실물 파일로 검증할 항목**이다.

1. ~~P열 위치 편차~~ — **해결됨.** 샘플 파일의 `P4:P35` 공유수식에 조합 문구가 들어 있음을
   확인했다(앞선 "N열에 있다"는 관찰은 XML 파싱 오류였다). N열은 비고칸으로,
   `신규` 표시가 들어가면 문구 앞에 `(신규) `가 붙고, 그 외 내용이 있으면 문구 끝에 ` (비고)`가 붙는다.
   → **P열 고정**, 다만 P열이 통째로 비어 있으면 B·C·D열 + 규격으로 직접 조합하는 대비책을 둔다.
2. **번호 체계 (확인 완료)** — 집계표 A열 번호는 **층이 바뀔 때마다 1로 리셋**된다.
   (옥상층 1~5 → 지상2층 1~15 → 지상1층 1~13) 따라서 `층키 + A열 번호` 가 곧 라벨이다.
3. **집계표 헤더 행** — 샘플은 데이터가 4행부터 시작(1~3행 머리글). 다른 파일에서
   시작 행이 다를 수 있으므로 `A열=1 + B열이 층 표기` 인 첫 행을 자동 탐지한다.
4. **조사위치 표기 변형** — `옥상층`/`지붕층`, `지상1층`/`1층` 등 표기가 섞일 수 있다.
   역변환 실패 시 해당 행을 미매칭 목록에 넣어 사용자에게 보여준다.
