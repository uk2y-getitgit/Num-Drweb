# 신규 프로젝트 구상 — 점검 데이터 · 보고서 자동화

> 작성 2026-08-22. **이 문서는 새 세션의 Claude가 처음 읽는 문서다.**
> 여기만 읽어도 무엇을 만들려는지, 무엇이 이미 있는지, 사람에게 무엇을 받아야 하는지 알 수 있게 썼다.
> 근거 자료: `docs/benchmark/젠트릭스_분석.md` (경쟁사 실측 기록 271줄)

---

## 0. 새 세션 Claude에게 — 먼저 할 일

1. 이 문서를 끝까지 읽는다
2. `docs/benchmark/젠트릭스_분석.md` 를 읽는다 — 경쟁 제품의 화면·데이터모델·출력물 실측이 있다
3. **§7 "사람에게 받아야 할 자료" 를 사용자에게 요청한다.** 이 자료 없이 서식을 추측해서 만들지 말 것.
   특히 손상결함표는 **발주처 제출용 법정 서식**이라 임의로 열을 정하면 못 쓴다
4. 그 다음에 설계를 시작한다

**이 저장소(Quickspect)에서 코드를 가져다 쓸 수 있다.** §5 참조.

---

## 1. 왜 별도 프로그램인가

기존 제품 **Quickspect**(구 NumDraw)는 "도면에 번호를 빠르게 찍고 사진첩을 뽑는 도구"다.
핵심 가치가 **2클릭으로 끝나는 가벼움**이다.

새로 만들려는 것은 "결함 하나하나에 속성 데이터를 붙이고 보고서 표를 생성하는 도구"다.
**성격이 반대다.** 입력 항목이 늘어날수록 무거워지는데, 그게 목적이다.

한 제품에 둘을 넣으면 둘 다 어중간해진다. 그래서 분리한다.

| | Quickspect (기존) | 신규 |
|---|---|---|
| 목적 | 넘버링 + 사진첩 | 점검 데이터 + 보고서 표 |
| 사용자 행동 | 클릭 2번 | 결함마다 속성 입력 |
| 산출물 | 조사위치도 PDF, 사진첩 PDF | 손상결함표(한글/엑셀), 집계 |
| 강점 | 가볍다, 오프라인, 일회성 구매 | 보고서 완성도 |

**두 제품이 나중에 만날 수는 있다.** 같은 프로젝트 파일을 주고받거나, 넘버링 결과를
신규 제품이 읽어들이는 식. 다만 처음부터 통합을 전제하지는 않는다.

---

## 2. 만들려는 것 (범위 초안)

```
[도면 불러오기] → [결함 표기 + 속성 입력] → [사진 연결] → [보고서 출력]
                        ↓                                    ↓
                  부재·결함유형·규모                   손상결함표(한글/엑셀)
                  진행·누수·발생원인                   조사위치도 · 사진첩
```

### 반드시 있어야 하는 것
- 결함 1건 = 레코드 1행. 도면 위 표기와 데이터가 1:1로 묶인다
- **손상결함표 출력** — 이게 이 제품의 존재 이유다
- 층·동 단위 관리 (번호 체계가 여기에 걸린다)
- 사진 연결 — **결함 번호와 사진 번호는 별개다**(§4 참조)

### 나중에
- 전회차 대조
- 현장 입력(태블릿)
- CAD 출력

---

## 3. 경쟁 제품 — 젠트릭스 (홈체크)

**같은 업을 하는 회사가 만들어 팔고 있다.** 발표자가 안전진단 전문기관 현직 차장이다.
`zentrix.kr` · Play/App Store · 웹앱(`app.zentrix.kr`). 상세는 `docs/benchmark/젠트릭스_분석.md`.

### 3-1. 결함 속성 데이터 모델 (실측)

```
부재(바닥슬래브/천장슬래브/파라펫/계단슬래브/지정벽돌/계단난간/
     천장마감재/드라이비트/외부벽체/담장/캐노피/경계석 …)
구조체 여부   : 비구조체 | 구조체
결함 유형     : 균열 | 파손 | 누수흔적 …
진행 여부     : 진행 없음 | 진행 중
규모          : 폭(mm) · 길이(mm) · 면적(㎡, 자동계산) · 개소(EA)
누수 여부     : O | X
발생원인 추정 : 번호 코드 또는 텍스트 (건조수축 / 이질재 접합 …)
보수 현황     :
사진          : n장
메모          :
```

### 3-2. 실제 손상결함표 출력물 (실측)

머리말 `건양대학교병원 | 제2장 현장조사 | <계 속>`, 층마다 `■ 지하1층` 섹션.

| NO | 위치 | 부재명 | 구조체 유형 | 결함의 유형 및 형상 | 폭(mm) | 길이(mm) | 면적(㎡) | 개소(EA) | 진행상황 | 누수여부 | 발생원인 추정 | 사진번호 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 93 | 지하1층 | 벽체 | 비구조체 | 수직균열 | 0.2 | 2000 | 0.0004 | 2 | O | X | 2 | 92 |
| 96 | 지하1층 | 상부슬래브 | 비구조체 | 누수흔적 | 0 | 0 | 0.5 | 1 | O | O | 3 | — |

**이 표를 만드는 것이 1차 목표다.** 단 실제 서식은 사람에게 원본을 받아 확인할 것(§7).

### 3-3. 입력 UI가 프리셋 중심이다

현장에서 키보드 없이 태블릿으로 찍는 것이 전제라 자유 입력보다 버튼이 먼저다.

- 부재: 버튼 그리드
- 폭: `0.1mm미만` `0.1~0.2` `0.2~0.3` `0.3~0.5` `0.5이상` `직접입력` 구간 버튼
- 길이/개수: `500` `1000` `3000` / `5` `10` 프리셋
- 발생원인: `건조수축` `이질재 접합` 프리셋

### 3-4. 회사별 맞춤 설정이 판매 포인트다

부재별로 어떤 결함을 띄울지, 어떤 단위로 입력할지를 **회사가 정한다.**
- 바닥슬래브·균열 → 폭·길이 입력, 기본 발생원인 "건조수축균열", 기본 보수 "표면처리공법"
- 벽체·망상균열 → **면적**으로 입력. 폭·길이를 넣으면 면적 자동 계산
- 회사가 쓰는 용어를 직접 추가

홈페이지 문구: 기존에 점검하던 용어와 방식을 그대로 쓸 수 있다는 것.
**도구에 사람을 맞추지 않는다** — 이 원칙이 이 시장에서 중요해 보인다.

### 3-5. 출력 옵션 (그대로 베낄 만하다)

```
외관조사 / 정밀점검 :  전체 | 외관조사만 | 정밀점검만
층 선택            :  층마다 1번부터 | 전체 번호가 이어서
                      [1 옥탑층][2 지상1층] … [15 지하4층]  ← 고른 순서 = 번호 순서
출력 옵션          :  이전회차 보수완료 항목  포함 | 포함하지 않음
단위               :  폭 mm|m   길이 mm|m
```

**층 칩을 클릭한 순서가 곧 번호 순서**라는 UX가 영리하다.
옥탑→지하냐 지하→옥탑이냐를 라디오가 아니라 "고른 순서"로 푼다.
출력 이력이 서버에 남고 재다운로드된다(한글본·엑셀본 구분).

### 3-6. 알아둘 것

- **젠트릭스도 AI 자동 보고서는 미구현이다.** `/docs` 에 "2026년 예정"으로 적혀 있는데
  홈페이지 히어로는 "AI가 작성해드립니다"라고 단언한다
- 가격 비공개. "기업 고객 상담" 폼과 전화(1522-5401)로만 안내. 트라이얼도 전화로 ID 배정
- 자동 줄맞춤·정렬 UI는 화면에서 확인되지 않았다 (번호는 수동 자유 배치)

---

## 4. 설계 시 놓치기 쉬운 것

### 4-1. 결함 번호 ≠ 사진 번호
출력표에서 `NO=93` 인데 `사진번호=92` 다. 두 번호가 독립적으로 매겨진다.
사진이 없는 결함도 있다(누수흔적 사례). **한 필드로 합치면 안 된다.**

### 4-2. 번호 체계가 출력 시점에 결정된다
결함을 입력할 때가 아니라 **출력할 때** 층 순서를 고르고 그에 따라 번호가 매겨진다.
즉 저장 데이터는 "순서 없는 결함 집합"이고, 번호는 출력 파라미터의 함수다.
처음부터 이렇게 잡지 않으면 나중에 갈아엎어야 한다.

### 4-3. 면적 자동 계산
폭 × 길이. 단위가 mm인데 결과는 ㎡다(0.2mm × 2000mm = 0.0004㎡). 단위 변환 주의.
부재·결함 조합에 따라 "면적으로 입력"과 "폭·길이로 입력"이 갈린다.

### 4-4. 발생원인 코드화
표에 숫자(2, 3)로 들어간다. 범례를 따로 두고 번호로 참조하는 방식.
텍스트로 쓰는 회사도 있다 — **둘 다 지원해야 한다.**

### 4-5. 개별 스타일이 전역 설정을 덮는다
젠트릭스 스타일 패널 안내: 스타일을 개별 변경하면 이후 전역 설정을 바꿔도 적용되지 않고,
[초기화]를 눌러야 전역에 다시 붙는다. Quickspect 의 `hatchColor` 와 같은 사상이다.

---

## 5. Quickspect 에서 가져다 쓸 수 있는 것

이 저장소에 이미 동작하는 코드가 있다. **처음부터 만들지 말 것.**

| 자산 | 위치 | 쓸모 |
|---|---|---|
| 도면 렌더 · 줌/팬 · 좌표계 | `js/canvas.js` | 그대로 재사용 가능 |
| 넘버링 데이터 모델 · 자동정렬 | `js/annotation.js` | 결함 레코드의 뼈대. **자동정렬은 젠트릭스에도 없는 우리 우위** |
| 페이지(층)별 독립 관리 · A4 변환 | `js/pageManager.js` | 층·동 구조에 그대로 맞는다 |
| **SheetJS 엑셀 읽기/쓰기** | `lib/xlsx.full.min.js`, `js/photoBook.js` | **손상결함표 엑셀 출력의 핵심.** 이미 외관집계표를 읽고 있다 |
| 사진 폴더 연결 · 파일명 번호 추출 · 매칭 | `js/fileManager.js` | 사진번호 연결 로직 |
| A4 격자 렌더 + PDF 출력 | `js/photoBook.js`, `lib/jspdf.umd.min.js` | 사진첩·조사위치도 출력 |
| 해치 도형(사각/원, 45° 빗금) | `js/canvas.js` | 젠트릭스의 "영역" 표기에 해당 |
| 도곽(표제란) | `js/titleBlock.js` | 보고서 도면 양식 |
| 자동저장 · IndexedDB | `js/storageManager.js` | |
| 라이선스 활성화 일체 | `electron/license/`, `server/` | **키 발급·2대 제한·증서 검증이 이미 돌아간다.** 판매까지 갈 거면 통째로 재사용 |
| 디자인 시스템(제도 콘솔) | `css/ui-tokens.css`, `css/ui-skin.css` | 토큰·컴포넌트 그대로 |

### 기술 스택 참고
Electron + 순수 HTML/CSS/Vanilla JS. 빌드 도구 없음. `index.html` 직접 열어도 돈다.
`CLAUDE.md` §4~§9에 규칙이 정리돼 있다.

---

## 6. 시장 판단

젠트릭스는 **점검 업무 전체를 대체하는 플랫폼**이다. 쓰려면 회사가 일하는 방식을
그 시스템으로 옮겨야 하고(과업 생성·계정·서버), 판매도 기업 상담이다.
**1인 사업자나 소규모 업체가 카드 긁듯 살 수 있는 물건이 아니다.**

신규 제품이 노릴 자리:
- 클라우드를 못 쓰거나 안 쓰는 곳 (로컬 전용)
- 일회성 구매를 원하는 소규모
- 쓰던 엑셀 양식을 그대로 유지하고 싶은 곳

**정면으로 기능을 다 따라가려 하지 말 것.** 손상결함표 하나를 제대로 뽑는 것만으로도 값이 있다.

---

## 7. 사람에게 받아야 할 자료 ⭐

**새 세션 Claude는 설계 전에 이것부터 요청할 것.** 없이 추측해서 만들면 다시 만들게 된다.

| # | 자료 | 왜 필요한가 | 없으면 |
|---|---|---|---|
| 1 | **손상결함표 한글(HWP) 실물** — 실제 제출본 1건 | 열 구성·병합·글꼴·여백이 법정 서식에 가깝다 | 열을 추측하게 되고 결과물이 못 쓰게 된다 |
| 2 | **같은 표의 엑셀본** | 우리가 먼저 만들 형식. 셀 구조를 그대로 맞춰야 한다 | |
| 3 | **정기안전점검 보고서 전체 1건** (가능하면 목차만이라도) | 손상결함표가 보고서 어디에 들어가는지, 앞뒤에 뭐가 필요한지 | 범위를 잘못 잡는다 |
| 4 | **부재명 표준 목록** — 회사에서 실제 쓰는 것 | 프리셋 버튼 구성 | |
| 5 | **결함 유형 목록** (수직균열·경사균열·망상균열·누수흔적 …) | 같음 | |
| 6 | **발생원인 범례표** — 번호↔원인 대응 | 표에 숫자로 들어가는 그 코드 | |
| 7 | **부재별 입력 단위 규칙** — 어떤 결함이 폭·길이이고 어떤 게 면적인가 | 자동 계산 분기 | |
| 8 | 회사마다 다른 부분이 무엇인지 (용어·번호 방식·서식) | 맞춤 설정의 범위 결정 | 과하게 유연하게 만들거나 너무 굳게 만든다 |
| 9 | 완성된 **외관조사망도** 실물 | 표기 스타일 기준 | |
| 10 | (있으면) 시설물안전법 서식 규정 근거 | 임의로 바꾸면 안 되는 부분 판별 | |

> 이미 이 저장소에 있는 것: `외관집계표_양식.xlsx` (단 `.gitignore` 대상이라 저장소엔 없다.
> 사람이 별도 경로로 전달해야 한다). 파서는 `js/photoBook.js` 에 이미 있다.

---

## 8. 아직 정하지 못한 것

- **플랫폼**: Electron 데스크톱? 웹? 태블릿까지?
  현장 입력을 넣을 거면 모바일이 필요한데, 그러면 서버가 따라온다
- **Quickspect 와의 관계**: 완전 별개 제품인가, 파일을 주고받는가, 언젠가 합치는가
- **판매 방식**: 기존 라이선스 체계(일회성·2대 제한)를 그대로 쓸 것인가
- **한글(HWP) 출력을 어떻게 할 것인가** — 이게 기술적으로 가장 큰 미지수다.
  브라우저/Electron 에서 HWP 를 직접 쓰는 표준 라이브러리가 마땅치 않다.
  대안: 엑셀만 제공 / HWP 서식 파일에 데이터만 채워 넣기 / ODT·DOCX 우회
  **1번 자료를 받아 실물을 본 뒤에 판단할 것**

---

## 9. 이 문서를 새 프로젝트로 옮길 때

이 파일과 `docs/benchmark/젠트릭스_분석.md` 두 개를 새 저장소로 복사하면 맥락이 따라간다.
`CLAUDE.md` 는 Quickspect 전용이므로 옮기지 말고, §5의 자산 목록을 보고
필요한 모듈만 골라 가져갈 것.
